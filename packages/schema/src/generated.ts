// Generated by packages/schema/scripts/generate.mjs from
// schemas/repodna-artifact.schema.json. Do not edit by hand: run `npm run generate -w @repodna/schema`.
/* eslint-disable */

/**
 * A single verifiable fact supporting an analysis result.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Evidence".
 */
export type Evidence =
  | {
      /**
       * Last line of the relevant range (1-based, inclusive).
       */
      endLine?: number | null;
      kind: "file";
      /**
       * First line of the relevant range (1-based).
       */
      line?: number | null;
      /**
       * Short explanation of why the file is relevant.
       */
      note?: string | null;
      /**
       * Repository-relative path.
       */
      path: string;
    }
  | {
      kind: "directory";
      /**
       * Short explanation of why the directory is relevant.
       */
      note?: string | null;
      /**
       * Repository-relative path of the directory (empty for the root).
       */
      path: string;
    }
  | {
      kind: "symbol";
      /**
       * Line where the symbol is defined (1-based).
       */
      line?: number | null;
      /**
       * Symbol name.
       */
      name: string;
      /**
       * Repository-relative path of the file that defines the symbol.
       */
      path: string;
    }
  | {
      /**
       * Commit timestamp.
       */
      date?: Timestamp | null;
      /**
       * Full commit hash.
       */
      hash: string;
      kind: "commit";
      /**
       * Commit subject line, when the privacy settings allow it.
       */
      summary?: string | null;
    }
  | {
      /**
       * Source of the edge (the dependent).
       */
      from: string;
      kind: "dependency-edge";
      /**
       * Line of the declaration (1-based).
       */
      line?: number | null;
      /**
       * File in which the relationship is declared.
       */
      path?: string | null;
      /**
       * Target of the edge (the dependency).
       */
      to: string;
    }
  | {
      /**
       * Package ecosystem, e.g. `cargo` or `npm`.
       */
      ecosystem: string;
      kind: "package";
      /**
       * Manifest or lockfile that declares the package.
       */
      manifest?: string | null;
      /**
       * Package name.
       */
      name: string;
      /**
       * Declared requirement or resolved version.
       */
      version?: string | null;
    }
  | {
      kind: "metric";
      /**
       * Metric identifier, e.g. `git.file.commits`.
       */
      metric: string;
      /**
       * Threshold the value was compared with.
       */
      threshold?: number | null;
      /**
       * Unit of the value, e.g. `lines`.
       */
      unit?: string | null;
      /**
       * Measured value.
       */
      value: number;
    }
  | {
      kind: "observation";
      /**
       * The observation.
       */
      text: string;
    };
/**
 * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Timestamp".
 */
export type Timestamp = string;
/**
 * Why a file was not analyzed beyond classification.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SkipReason".
 */
export type SkipReason = "too-large" | "unreadable";
/**
 * Overall activity level at the time of analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ActivityLevel".
 */
export type ActivityLevel = "very-active" | "active" | "moderate" | "low" | "dormant" | "none";
/**
 * Approximate age class of a module relative to the repository's lifetime.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AgeClass".
 */
export type AgeClass = "ancient" | "established" | "growing" | "recent" | "new";
/**
 * A preset trading analysis depth for speed.
 *
 * All profiles are deterministic.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AnalysisProfile".
 */
export type AnalysisProfile =
  | "quick"
  | "standard"
  | "deep"
  | "history-only"
  | "architecture-only"
  | "dependencies-only"
  | "security-only";
/**
 * Outcome of one pipeline stage.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AnalyzerStatus".
 */
export type AnalyzerStatus = "completed" | "partial" | "skipped" | "failed" | "cancelled";
/**
 * How a dependency changed.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ChangeKind".
 */
export type ChangeKind = "added" | "removed" | "changed";
/**
 * What a command is for, in the order a newcomer would run them.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CommandPurpose".
 */
export type CommandPurpose =
  "install" | "build" | "test" | "run" | "dev" | "lint" | "format" | "bench";
/**
 * How strongly the available evidence supports an analysis result.
 *
 * Confidence describes the *method*, not the repository: a dependency edge resolved from
 * an explicit relative import is `High`, while a module boundary inferred from directory
 * names is `Medium`. `Unavailable` means the analysis could not be performed at all, which
 * is different from "nothing was detected".
 *
 * Variants are ordered from weakest to strongest, so `Confidence::Low < Confidence::High`.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Confidence".
 */
export type Confidence = "unavailable" | "low" | "medium" | "high";
/**
 * The graph level at which a cycle was found.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CycleLevel".
 */
export type CycleLevel = "module" | "file";
/**
 * Kind of data source used by the analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DataSourceKind".
 */
export type DataSourceKind =
  "local-files" | "git-history" | "remote-clone" | "plugin" | "ai" | "advisory";
/**
 * Kind of a dead-code candidate.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DeadCodeKind".
 */
export type DeadCodeKind = "unreferenced-file" | "unreferenced-module" | "legacy-directory";
/**
 * The scope in which a dependency is used.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyScope".
 */
export type DependencyScope = "runtime" | "development" | "build" | "optional" | "peer";
/**
 * Result of a documentation check.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DocCheckStatus".
 */
export type DocCheckStatus = "present" | "partial" | "not-detected";
/**
 * The syntactic mechanism behind a file-level dependency edge.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EdgeKind".
 */
export type EdgeKind = "import" | "include" | "module" | "reference" | "package";
/**
 * The kind of an entrypoint.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EntrypointKind".
 */
export type EntrypointKind = "binary" | "library" | "web" | "cli" | "server" | "script";
/**
 * Kind of a development epoch. Labels are interpretations of commit activity.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EpochKind".
 */
export type EpochKind = "initial" | "active" | "maintenance" | "current";
/**
 * Kind of an evolution event.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EvolutionEventKind".
 */
export type EvolutionEventKind =
  | "repository-created"
  | "module-introduced"
  | "module-removed"
  | "restructuring"
  | "language-introduced"
  | "language-shift"
  | "framework-adopted"
  | "framework-removed"
  | "tests-introduced"
  | "ci-adopted"
  | "containers-adopted"
  | "significant-growth"
  | "significant-reduction"
  | "multi-package-structure"
  | "release";
/**
 * The primary role of a file in the repository.
 *
 * When several categories apply, the most specific one wins; see the discovery crate for
 * the precedence rules. Independent flags on [`FileRecord`] (`generated`, `vendored`,
 * `binary`) record orthogonal properties.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileCategory".
 */
export type FileCategory =
  | "source"
  | "test"
  | "documentation"
  | "configuration"
  | "ci-cd"
  | "build"
  | "manifest"
  | "lockfile"
  | "generated"
  | "asset"
  | "binary"
  | "vendor"
  | "build-output"
  | "ide-metadata"
  | "container"
  | "infrastructure"
  | "data"
  | "license"
  | "other";
/**
 * The dashboard area a finding belongs to.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FindingCategory".
 */
export type FindingCategory =
  | "structure"
  | "architecture"
  | "dependencies"
  | "activity"
  | "contributors"
  | "complexity"
  | "duplication"
  | "maintainability"
  | "tests"
  | "build"
  | "documentation"
  | "security"
  | "evolution"
  | "plugin";
/**
 * How the repository was provided.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "InputKind".
 */
export type InputKind = "local-directory" | "git-repository" | "git-url" | "archive" | "artifact";
/**
 * Broad family of a language, used to decide which languages count toward composition shares.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LanguageKind".
 */
export type LanguageKind = "programming" | "markup" | "stylesheet" | "data" | "prose" | "build";
/**
 * Role of a dependency file.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ManifestKind".
 */
export type ManifestKind = "manifest" | "lockfile";
/**
 * Kind of a code marker comment.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MarkerKind".
 */
export type MarkerKind = "todo" | "fixme" | "hack" | "xxx" | "bug" | "deprecated";
/**
 * How a module boundary was established.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ModuleKind".
 */
export type ModuleKind = "package" | "directory" | "root";
/**
 * Outcome of parsing a dependency file.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ParseStatus".
 */
export type ParseStatus = "parsed" | "partial" | "failed";
/**
 * How deeply RepoDNA can analyze files of a language.
 *
 * RepoDNA never claims more than it does: the built-in analyzers are *lexical* (they
 * understand comments, strings, imports, and block structure through tokenization and
 * patterns), not full parsers. `Syntactic` and `Semantic` are reserved for parser plugins.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ParserCapability".
 */
export type ParserCapability = "detection" | "line-count" | "lexical" | "syntactic" | "semantic";
/**
 * Category of a risky pattern.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PatternCategory".
 */
export type PatternCategory = "code" | "configuration" | "workflow" | "container";
/**
 * How much identifying data exported reports contain.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PrivacyPreset".
 */
export type PrivacyPreset = "local" | "share" | "public";
/**
 * Kind of environment requirement.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "RequirementKind".
 */
export type RequirementKind =
  "runtime" | "toolchain" | "package-manager" | "sdk" | "container" | "platform" | "service";
/**
 * Whether an analysis section was produced, and how completely.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SectionStatus".
 */
export type SectionStatus = "analyzed" | "partial" | "skipped" | "unavailable";
/**
 * How urgently a finding deserves attention.
 *
 * Severity is never assigned arbitrarily: every analyzer maps its findings to one of these
 * levels using the criteria returned by [`Severity::criteria`], which are also published in
 * the documentation. Most quality signals are `Info` or `Attention`, because a metric on its
 * own rarely proves a defect.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Severity".
 */
export type Severity = "info" | "attention" | "warning" | "critical";
/**
 * Repository size class by number of files.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SizeClass".
 */
export type SizeClass = "tiny" | "small" | "medium" | "large" | "very-large";
/**
 * Why a revision was chosen as a Time Machine snapshot.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SnapshotKind".
 */
export type SnapshotKind = "initial" | "release" | "sample" | "current";
/**
 * Whether a statement is a verifiable fact or an interpretation.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "StatementKind".
 */
export type StatementKind = "fact" | "interpretation";
/**
 * The kind of a detected symbol.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SymbolKind".
 */
export type SymbolKind =
  | "function"
  | "method"
  | "class"
  | "struct"
  | "enum"
  | "interface"
  | "trait"
  | "module"
  | "type"
  | "constant"
  | "macro"
  | "schema";
/**
 * Kind of automated tests.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "TestKind".
 */
export type TestKind = "unit" | "integration" | "end-to-end" | "snapshot" | "benchmark";

/**
 * RepoDNA artifact, schema version 1.0. Readers must ignore unknown fields.
 */
export interface RepositoryDna {
  analysisMetadata: AnalysisMetadata;
  architecture: ArchitectureReport;
  builds: BuildReport;
  codeQuality: QualityReport;
  /**
   * Declared dependencies.
   */
  dependencies: {
    advisories: AdvisoryStatus;
    /**
     * Most frequently imported external packages.
     */
    concentration: DependencyConcentration[];
    /**
     * Direct dependencies, sorted by ecosystem and name.
     */
    dependencies: DependencyRecord[];
    /**
     * Number of distinct direct dependencies.
     */
    directCount: number;
    /**
     * Packages resolved at multiple versions.
     */
    duplicates: DuplicateVersions[];
    /**
     * Counts per ecosystem.
     */
    ecosystems: EcosystemSummary[];
    /**
     * Number of distinct packages pinned in lockfiles (direct and transitive).
     */
    lockedCount: number;
    /**
     * Lockfiles with consistency information.
     */
    lockfiles: LockfileRecord[];
    /**
     * Manifests and lockfiles found.
     */
    manifests: ManifestRecord[];
    /**
     * Notes about limitations or partial results.
     */
    notes?: string[];
    /**
     * Long-unchanged manifests.
     */
    staleSignals: StaleSignal[];
    /**
     * Whether this section was analyzed.
     */
    status: "analyzed" | "partial" | "skipped" | "unavailable";
  };
  docs: DocsReport;
  evolution: EvolutionReport;
  /**
   * Evidence-backed findings, most severe first.
   */
  findings: Finding[];
  fingerprint: DnaFingerprint;
  /**
   * Reports generated alongside this artifact.
   */
  generatedReports: GeneratedReport[];
  git: GitReport;
  identity: RepositoryIdentity;
  insights: Insights;
  languages: LanguageReport;
  metrics: MetricsReport;
  /**
   * Plugins that ran.
   */
  plugins: PluginRunRecord[];
  /**
   * Artifact schema version, e.g. `1.0`.
   */
  schemaVersion: string;
  security: SecurityReport;
  similarity: SimilarityReport;
  structure: StructureReport;
  tests: TestReport;
  tool: ToolInfo;
}
/**
 * How the artifact was produced.
 */
export interface AnalysisMetadata {
  /**
   * AI usage, when an AI provider was used.
   */
  ai?: AiUsage | null;
  /**
   * Per-stage outcomes and timings.
   */
  analyzers: AnalyzerRun[];
  /**
   * Hash of the effective configuration.
   */
  configHash: string;
  /**
   * Configuration layers that contributed.
   */
  configSources: string[];
  /**
   * Data sources used.
   */
  dataSources: DataSource[];
  /**
   * `true` when the working tree had uncommitted changes.
   */
  dirty?: boolean | null;
  /**
   * Total wall-clock duration in milliseconds.
   */
  durationMs: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  generatedAt: string;
  /**
   * Identifier of this analysis run.
   */
  id: string;
  input: InputInfo;
  /**
   * `true` when the analysis was cancelled or a stage failed.
   */
  partial: boolean;
  platform: PlatformInfo;
  privacy: PrivacyInfo;
  /**
   * Profile used.
   */
  profile:
    | "quick"
    | "standard"
    | "deep"
    | "history-only"
    | "architecture-only"
    | "dependencies-only"
    | "security-only";
  /**
   * Commit analyzed, when available.
   */
  revision?: string | null;
  /**
   * Findings suppressed by those rules.
   */
  suppressedFindings: number;
  /**
   * Suppression rules in effect.
   */
  suppressions: SuppressionRule[];
  thresholds: Thresholds;
  /**
   * Warnings raised during the analysis.
   */
  warnings: string[];
}
/**
 * AI usage during the analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AiUsage".
 */
export interface AiUsage {
  /**
   * Model identifier.
   */
  model: string;
  /**
   * Provider identifier.
   */
  provider: string;
  /**
   * `true` when data left the machine.
   */
  remote: boolean;
  /**
   * Requests made.
   */
  requests: number;
}
/**
 * Timing and outcome of one pipeline stage.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AnalyzerRun".
 */
export interface AnalyzerRun {
  /**
   * Wall-clock duration in milliseconds.
   */
  durationMs: number;
  /**
   * Human-readable label.
   */
  label: string;
  /**
   * Explanation for partial, failed, or skipped stages.
   */
  message?: string | null;
  /**
   * Stage identifier, e.g. `git`.
   */
  stage: string;
  /**
   * Outcome.
   */
  status: "completed" | "partial" | "skipped" | "failed" | "cancelled";
}
/**
 * A data source used by the analysis, with the time it was accessed.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DataSource".
 */
export interface DataSource {
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  accessedAt: string;
  /**
   * Additional detail.
   */
  detail: string;
  /**
   * Source kind.
   */
  kind: "local-files" | "git-history" | "remote-clone" | "plugin" | "ai" | "advisory";
  /**
   * Name, e.g. `git 2.43.0`.
   */
  name: string;
}
/**
 * Input description.
 */
export interface InputInfo {
  /**
   * Safe display string: a directory name, a credential-free URL, or an archive file name.
   */
  display: string;
  /**
   * Input kind.
   */
  kind: "local-directory" | "git-repository" | "git-url" | "archive" | "artifact";
}
/**
 * Platform.
 */
export interface PlatformInfo {
  /**
   * CPU architecture, e.g. `x86_64`.
   */
  arch: string;
  /**
   * OS family, e.g. `unix`.
   */
  family: string;
  /**
   * Operating system, e.g. `linux`.
   */
  os: string;
}
/**
 * Privacy facts.
 */
export interface PrivacyInfo {
  /**
   * `true` when the analysis used the network (for example to clone a URL).
   */
  networkUsed: boolean;
  /**
   * Privacy preset applied to this document.
   */
  preset: "local" | "share" | "public";
  /**
   * Redactions applied to this document, e.g. `contributor names anonymized`.
   */
  redactions: string[];
  /**
   * `true` when a remote AI provider received data.
   */
  remoteAi: boolean;
  /**
   * Always `false`: RepoDNA collects no telemetry.
   */
  telemetry: boolean;
}
/**
 * "Ignore finding X in path Y" — a user-defined suppression rule.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SuppressionRule".
 */
export interface SuppressionRule {
  /**
   * Optional exact finding identifier.
   */
  id?: string | null;
  /**
   * Optional path glob; the rule then applies only to findings about matching paths.
   */
  path?: string | null;
  /**
   * Why the finding is suppressed. Required so suppressions stay explainable.
   */
  reason: string;
  /**
   * Rule identifier or wildcard pattern, e.g. `security.secret` or `quality.*`.
   */
  rule: string;
  /**
   * Where the rule was defined; filled in when configuration is loaded.
   */
  source?: string | null;
}
/**
 * Thresholds in effect.
 */
export interface Thresholds {
  /**
   * Block nesting depth above which a function is reported.
   */
  deep_nesting: number;
  /**
   * Share of internal module edges (0–1) pointing at one module above which a
   * dependency-concentration signal is reported.
   */
  dependency_concentration: number;
  /**
   * Minimum gap in days to report a dormant period.
   */
  dormant_days: number;
  /**
   * Minimum duplicated block size in normalized tokens.
   */
  duplicate_min_tokens: number;
  /**
   * Share of recent churn (0–1) above which a directory is reported as a high-churn area.
   */
  high_churn_share: number;
  /**
   * Approximate cyclomatic complexity above which a function is reported.
   */
  high_complexity: number;
  /**
   * Minimum commits for a file to be considered as a hotspot.
   */
  hotspot_min_commits: number;
  /**
   * Size in bytes above which a committed binary file is reported.
   */
  large_binary_bytes: number;
  /**
   * Code lines above which a file is reported as large.
   */
  large_file_lines: number;
  /**
   * Lines above which a function is reported as large.
   */
  large_function_lines: number;
  /**
   * Size of the "recent" window in days (before the latest commit).
   */
  recent_days: number;
  /**
   * Minimum estimated Jaccard similarity (0–1) for similar-file pairs.
   */
  similarity: number;
  /**
   * Days a manifest must be unchanged before a stale-declarations signal is reported.
   */
  stale_manifest_days: number;
}
/**
 * Inferred architecture.
 */
export interface ArchitectureReport {
  /**
   * Dependency cycles.
   */
  cycles: DependencyCycle[];
  /**
   * External packages imported by the code.
   */
  external: ExternalImport[];
  /**
   * File-level edges (capped; see `fileEdgesTruncated`).
   */
  fileEdges: FileEdge[];
  /**
   * `true` when `fileEdges` was capped.
   */
  fileEdgesTruncated?: boolean;
  /**
   * Modules with no internal edges in either direction.
   */
  isolatedModules: string[];
  /**
   * Layers of the condensed module graph.
   */
  layers: Layer[];
  /**
   * Description of the inference method.
   */
  method: string;
  /**
   * Module-level edges.
   */
  moduleEdges: ModuleEdge[];
  /**
   * Modules, sorted by identifier.
   */
  modules: ModuleRecord[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Package boundaries (monorepo members).
   */
  packages: PackageBoundary[];
  /**
   * Import statements resolved to repository files.
   */
  resolvedImports: number;
  /**
   * Signals that led to the characterization.
   */
  signals: ArchitectureSignal[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Short characterization such as `Modular` or `Layered`; always labeled as inferred.
   */
  style: string;
  /**
   * Confidence of the characterization.
   */
  styleConfidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Import statements that referenced neither a repository file nor a known package.
   */
  unresolvedImports: number;
  /**
   * Workspace configuration, when present.
   */
  workspace?: WorkspaceInfo | null;
}
/**
 * A dependency cycle (a strongly connected component with more than one member).
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyCycle".
 */
export interface DependencyCycle {
  /**
   * Confidence (the weakest confidence of the edges on `path`).
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * The edges along `path`.
   */
  evidence: Evidence[];
  /**
   * Stable identifier derived from the sorted members.
   */
  id: string;
  /**
   * Languages of the files involved.
   */
  languages: string[];
  /**
   * Graph level.
   */
  level: "module" | "file";
  /**
   * All members of the strongly connected component, sorted.
   */
  members: string[];
  /**
   * A shortest concrete cycle through the component; the first node is repeated at the end.
   */
  path: string[];
}
/**
 * An external package imported by repository code.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ExternalImport".
 */
export interface ExternalImport {
  /**
   * Ecosystem inferred from the importing language, when known.
   */
  ecosystem?: string | null;
  /**
   * Number of files importing the package.
   */
  importers: number;
  /**
   * Modules importing the package.
   */
  modules: string[];
  /**
   * Package or module name, e.g. `serde` or `react`.
   */
  name: string;
  /**
   * Sample import locations.
   */
  samples: Evidence[];
}
/**
 * A dependency edge between two repository files.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileEdge".
 */
export interface FileEdge {
  /**
   * Confidence of the resolution.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Dependent file.
   */
  from: string;
  /**
   * Mechanism.
   */
  kind: "import" | "include" | "module" | "reference" | "package";
  /**
   * Line of the declaration in `from` (1-based).
   */
  line: number;
  /**
   * The import specifier as written, truncated to 120 characters.
   */
  specifier?: string | null;
  /**
   * Dependency file.
   */
  to: string;
}
/**
 * A layer of the condensed module dependency graph.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Layer".
 */
export interface Layer {
  /**
   * Layer index; modules in layer 0 depend on no other internal module.
   */
  index: number;
  /**
   * Module identifiers in the layer.
   */
  modules: string[];
}
/**
 * A dependency edge between two modules, aggregated from file edges.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ModuleEdge".
 */
export interface ModuleEdge {
  /**
   * Strongest confidence among the supporting file edges.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Dependent module identifier.
   */
  from: string;
  /**
   * Sample of the supporting file edges.
   */
  samples: Evidence[];
  /**
   * Dependency module identifier.
   */
  to: string;
  /**
   * Number of file-level edges supporting this module edge.
   */
  weight: number;
}
/**
 * A component of the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ModuleRecord".
 */
export interface ModuleRecord {
  /**
   * Normalized betweenness centrality in the module graph (0–1).
   */
  centrality: number;
  /**
   * Code lines.
   */
  codeLines: number;
  /**
   * Confidence of the boundary.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Evidence for the module boundary.
   */
  evidence: Evidence[];
  /**
   * External packages imported from this module.
   */
  externalDependencies?: string[];
  /**
   * Number of distinct modules that depend on this module.
   */
  fanIn: number;
  /**
   * Number of distinct modules this module depends on.
   */
  fanOut: number;
  /**
   * Number of files.
   */
  files: number;
  /**
   * Stable identifier (normally the module's root path, or `(root)`).
   */
  id: string;
  /**
   * Evidence-backed reasons why this module matters ("18 modules depend on it").
   */
  importance?: string[];
  /**
   * `true` when the boundary is inferred rather than declared.
   */
  inferred: boolean;
  /**
   * `fan_out / (fan_in + fan_out)`: 0 means only depended upon, 1 means only depending.
   */
  instability: number;
  /**
   * How the boundary was established.
   */
  kind: "package" | "directory" | "root";
  /**
   * Language with the most code lines in the module.
   */
  language?: string | null;
  /**
   * Layer index in the condensed dependency graph (0 = depends on no other module).
   */
  layer?: number | null;
  /**
   * Display name (package name or directory name).
   */
  name: string;
  /**
   * Repository-relative root directory of the module.
   */
  path: string;
}
/**
 * A package boundary inside a monorepo or workspace.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PackageBoundary".
 */
export interface PackageBoundary {
  /**
   * Ecosystem, e.g. `cargo`.
   */
  ecosystem: string;
  /**
   * Other workspace packages this package depends on.
   */
  internalDependencies: string[];
  /**
   * Manifest declaring the package.
   */
  manifest: string;
  /**
   * Package name as declared in its manifest.
   */
  name: string;
  /**
   * Repository-relative directory.
   */
  path: string;
}
/**
 * An architecture-style signal with its evidence.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ArchitectureSignal".
 */
export interface ArchitectureSignal {
  /**
   * Confidence.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Explanation.
   */
  description: string;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Identifier, e.g. `monorepo`.
   */
  id: string;
  /**
   * Short label.
   */
  label: string;
}
/**
 * A workspace (monorepo) configuration.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "WorkspaceInfo".
 */
export interface WorkspaceInfo {
  /**
   * File declaring the workspace.
   */
  manifest: string;
  /**
   * Member patterns as declared.
   */
  members: string[];
  /**
   * Workspace tool, e.g. `cargo-workspace` or `npm-workspaces`.
   */
  tool: string;
}
/**
 * Build systems, environment, and CI.
 */
export interface BuildReport {
  /**
   * CI/CD configurations.
   */
  ci: CiConfig[];
  /**
   * Build, run, and development commands derived from metadata.
   */
  commands: CommandCandidate[];
  /**
   * Configuration files and their purpose.
   */
  configurationFiles: ConfigFileInfo[];
  /**
   * Container definitions.
   */
  containers: string[];
  /**
   * Results of explicitly authorized build runs.
   */
  executions: ExecutionResult[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Detected environment requirements.
   */
  requirements: EnvironmentRequirement[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Detected build systems.
   */
  systems: DetectedTool[];
}
/**
 * A CI/CD configuration.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CiConfig".
 */
export interface CiConfig {
  /**
   * Job or stage names found in the file.
   */
  jobs: string[];
  /**
   * Configuration file.
   */
  path: string;
  /**
   * Provider, e.g. `GitHub Actions`.
   */
  provider: string;
}
/**
 * A command RepoDNA derived from repository metadata.
 *
 * Candidates are *detected*, not *verified*: RepoDNA never runs them unless the user
 * explicitly enables command execution, and `verified` is only set by a successful run.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CommandCandidate".
 */
export interface CommandCandidate {
  /**
   * The command line.
   */
  command: string;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * What the command is for.
   */
  purpose: "install" | "build" | "test" | "run" | "dev" | "lint" | "format" | "bench";
  /**
   * Where the command was found, e.g. `package.json scripts.test`.
   */
  source: string;
  /**
   * `true` only after the command ran successfully under RepoDNA's supervision.
   */
  verified?: boolean;
  /**
   * Repository-relative working directory (empty for the root).
   */
  workingDirectory: string;
}
/**
 * A configuration file and its purpose.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ConfigFileInfo".
 */
export interface ConfigFileInfo {
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * What the file configures.
   */
  purpose: string;
}
/**
 * Outcome of an explicitly authorized command execution.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ExecutionResult".
 */
export interface ExecutionResult {
  /**
   * `true` when the user cancelled the run.
   */
  cancelled?: boolean;
  /**
   * The command line that was executed.
   */
  command: string;
  /**
   * Wall-clock duration in milliseconds.
   */
  durationMs: number;
  /**
   * Process exit code, absent when the process was killed.
   */
  exitCode?: number | null;
  /**
   * Last lines of combined output, with secret-like values redacted.
   */
  outputTail: string[];
  /**
   * What the command was for.
   */
  purpose: "install" | "build" | "test" | "run" | "dev" | "lint" | "format" | "bench";
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  startedAt: string;
  /**
   * `true` when the process exited with code 0.
   */
  success: boolean;
  /**
   * `true` when the timeout was reached and the process was stopped.
   */
  timedOut?: boolean;
  /**
   * Repository-relative working directory.
   */
  workingDirectory: string;
}
/**
 * A detected project requirement.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EnvironmentRequirement".
 */
export interface EnvironmentRequirement {
  /**
   * Requirement kind.
   */
  kind: "runtime" | "toolchain" | "package-manager" | "sdk" | "container" | "platform" | "service";
  /**
   * Name, e.g. `Node.js`.
   */
  name: string;
  /**
   * File declaring the requirement.
   */
  source: string;
  /**
   * Declared version or constraint, when present.
   */
  version?: string | null;
}
/**
 * A tool, framework, or build system detected in the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DetectedTool".
 */
export interface DetectedTool {
  /**
   * Confidence of the detection.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Ecosystem, when meaningful.
   */
  ecosystem?: string | null;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Tool name, e.g. `cargo test` or `vitest`.
   */
  name: string;
}
/**
 * Code-quality signals.
 */
export interface QualityReport {
  complexity: ComplexityReport;
  /**
   * Dead-code candidates.
   */
  deadCodeCandidates: DeadCodeCandidate[];
  /**
   * Functions with deep nesting.
   */
  deepNesting: FunctionSignal[];
  duplication: DuplicationReport;
  /**
   * Files above the large-file threshold.
   */
  largeFiles: FileSignal[];
  /**
   * Functions above the large-function threshold.
   */
  largeFunctions: FunctionSignal[];
  /**
   * Maintainability indicators.
   */
  maintainability: MaintainabilitySignal[];
  markers: MarkerReport;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Complexity signals.
 */
export interface ComplexityReport {
  /**
   * Mean approximate cyclomatic complexity.
   */
  averageCyclomatic: number;
  /**
   * Statistics per language.
   */
  byLanguage: LanguageComplexity[];
  /**
   * Distribution of function complexity.
   */
  distribution: DistributionBucket[];
  /**
   * Functions analyzed.
   */
  functionsAnalyzed: number;
  /**
   * Median approximate cyclomatic complexity.
   */
  medianCyclomatic: number;
  /**
   * How complexity was approximated.
   */
  method: string;
  /**
   * Most complex functions, descending.
   */
  topFunctions: FunctionSignal[];
}
/**
 * Complexity statistics for one language.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LanguageComplexity".
 */
export interface LanguageComplexity {
  /**
   * Mean approximate cyclomatic complexity.
   */
  average: number;
  /**
   * Functions analyzed.
   */
  functions: number;
  /**
   * Language identifier.
   */
  language: string;
  /**
   * Highest approximate cyclomatic complexity.
   */
  max: number;
}
/**
 * One bucket of a distribution histogram.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DistributionBucket".
 */
export interface DistributionBucket {
  /**
   * Number of items in the bucket.
   */
  count: number;
  /**
   * Label such as `1–5` or `51+`.
   */
  label: string;
  /**
   * Inclusive upper bound; absent for the open-ended last bucket.
   */
  max?: number | null;
  /**
   * Inclusive lower bound.
   */
  min: number;
}
/**
 * A function-level measurement.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FunctionSignal".
 */
export interface FunctionSignal {
  /**
   * Approximate cyclomatic complexity.
   */
  cyclomatic: number;
  /**
   * Last line (1-based, inclusive).
   */
  endLine: number;
  /**
   * Language identifier.
   */
  language: string;
  /**
   * First line (1-based).
   */
  line: number;
  /**
   * Length in lines.
   */
  lines: number;
  /**
   * Function name.
   */
  name: string;
  /**
   * Deepest block nesting inside the function.
   */
  nesting: number;
  /**
   * Repository-relative path.
   */
  path: string;
}
/**
 * A conservative dead-code candidate. Never a definitive claim.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DeadCodeCandidate".
 */
export interface DeadCodeCandidate {
  /**
   * Confidence.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Candidate kind.
   */
  kind: "unreferenced-file" | "unreferenced-module" | "legacy-directory";
  /**
   * Label shown to users, e.g. `No references detected`.
   */
  label: string;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Why the path was flagged.
   */
  reason: string;
}
/**
 * Duplication analysis.
 */
export interface DuplicationReport {
  /**
   * Code lines considered for duplication.
   */
  analyzedLines: number;
  /**
   * Duplicate clusters, largest first.
   */
  clusters: DuplicateCluster[];
  /**
   * Lines covered by at least one duplicated block (excluding the first occurrence).
   */
  duplicatedLines: number;
  /**
   * How duplication was detected.
   */
  method: string;
  /**
   * Minimum block size in normalized tokens.
   */
  minTokens: number;
  /**
   * `duplicatedLines / analyzedLines`.
   */
  ratio: number;
  /**
   * Whether duplication detection ran.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * A set of code blocks with identical normalized token sequences.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DuplicateCluster".
 */
export interface DuplicateCluster {
  /**
   * Stable identifier.
   */
  id: string;
  /**
   * Language identifier.
   */
  language: string;
  /**
   * Lines in the (first) duplicated block.
   */
  lines: number;
  /**
   * Total number of occurrences of the block.
   */
  occurrenceCount: number;
  /**
   * Occurrences of the block, in path order (capped; see `occurrenceCount`).
   */
  occurrences: CodeLocation[];
  /**
   * Normalized tokens in the duplicated block.
   */
  tokens: number;
}
/**
 * A code location.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CodeLocation".
 */
export interface CodeLocation {
  /**
   * Last line (1-based, inclusive).
   */
  endLine: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * First line (1-based).
   */
  startLine: number;
}
/**
 * A file-level measurement compared with a threshold.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileSignal".
 */
export interface FileSignal {
  /**
   * Language identifier.
   */
  language?: string | null;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Threshold the value exceeded.
   */
  threshold: number;
  /**
   * Unit of `value`, e.g. `lines`.
   */
  unit: string;
  /**
   * Measured value.
   */
  value: number;
}
/**
 * A maintainability indicator with its interpretation.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MaintainabilitySignal".
 */
export interface MaintainabilitySignal {
  /**
   * Neutral description of what the value means.
   */
  description: string;
  /**
   * Identifier.
   */
  id: string;
  /**
   * Label.
   */
  label: string;
  /**
   * Unit of `value`.
   */
  unit: string;
  /**
   * Measured value.
   */
  value: number;
}
/**
 * Marker comments.
 */
export interface MarkerReport {
  /**
   * Counts per kind.
   */
  counts: MarkerCount[];
  /**
   * Individual markers (capped).
   */
  items: MarkerItem[];
  /**
   * Total markers found.
   */
  total: number;
}
/**
 * Number of markers of one kind.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MarkerCount".
 */
export interface MarkerCount {
  /**
   * Occurrences.
   */
  count: number;
  /**
   * Marker kind.
   */
  kind: "todo" | "fixme" | "hack" | "xxx" | "bug" | "deprecated";
}
/**
 * One marker comment.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MarkerItem".
 */
export interface MarkerItem {
  /**
   * Marker kind.
   */
  kind: "todo" | "fixme" | "hack" | "xxx" | "bug" | "deprecated";
  /**
   * Line (1-based).
   */
  line: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Comment text, truncated and with secret-like values redacted.
   */
  text: string;
}
/**
 * Advisory integration status.
 */
export interface AdvisoryStatus {
  /**
   * When the advisory data was retrieved.
   */
  checkedAt?: Timestamp | null;
  /**
   * Explanation, e.g. that no provider was configured.
   */
  note: string;
  /**
   * Advisory provider used, if any.
   */
  provider?: string | null;
}
/**
 * How often an external package is imported by repository code.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyConcentration".
 */
export interface DependencyConcentration {
  /**
   * Files importing it.
   */
  importers: number;
  /**
   * Package name.
   */
  name: string;
  /**
   * Share of all external-import sites (0–1).
   */
  share: number;
}
/**
 * A direct dependency declared in one or more manifests.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyRecord".
 */
export interface DependencyRecord {
  /**
   * Ecosystem identifier.
   */
  ecosystem: string;
  /**
   * `true` when the dependency is another package of the same repository.
   */
  internal?: boolean;
  /**
   * Manifests declaring the dependency.
   */
  manifests: string[];
  /**
   * Package name.
   */
  name: string;
  /**
   * Declared version requirement, as written.
   */
  requirement?: string | null;
  /**
   * Versions resolved in lockfiles.
   */
  resolved?: string[];
  /**
   * Usage scope.
   */
  scope: "runtime" | "development" | "build" | "optional" | "peer";
  /**
   * Source kind: `registry`, `git`, `path`, or `workspace`.
   */
  source: string;
}
/**
 * A package resolved at several versions in one lockfile.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DuplicateVersions".
 */
export interface DuplicateVersions {
  /**
   * Ecosystem identifier.
   */
  ecosystem: string;
  /**
   * Lockfile containing the duplicates.
   */
  lockfile: string;
  /**
   * Package name.
   */
  name: string;
  /**
   * Distinct resolved versions, sorted.
   */
  versions: string[];
}
/**
 * Counts per ecosystem.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EcosystemSummary".
 */
export interface EcosystemSummary {
  /**
   * Ecosystem identifier.
   */
  ecosystem: string;
  /**
   * Packages pinned across lockfiles (direct and transitive).
   */
  lockedPackages: number;
  /**
   * Lockfiles found.
   */
  lockfiles: number;
  /**
   * Manifests found.
   */
  manifests: number;
  /**
   * Direct development, build, optional, and peer dependencies.
   */
  other: number;
  /**
   * Direct runtime dependencies.
   */
  runtime: number;
}
/**
 * A lockfile and its consistency with the manifests it belongs to.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LockfileRecord".
 */
export interface LockfileRecord {
  /**
   * Ecosystem identifier.
   */
  ecosystem: string;
  /**
   * Explanation when parsing was partial or failed.
   */
  message?: string | null;
  /**
   * Direct registry dependencies of the sibling manifest that the lockfile does not contain.
   */
  missingFromLock?: string[];
  /**
   * Packages pinned.
   */
  packages: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Parse outcome.
   */
  status: "parsed" | "partial" | "failed";
}
/**
 * A manifest or lockfile.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ManifestRecord".
 */
export interface ManifestRecord {
  /**
   * Declared package description.
   */
  description?: string | null;
  /**
   * Ecosystem identifier, e.g. `cargo`, `npm`, or `pypi`.
   */
  ecosystem: string;
  /**
   * Number of dependencies declared (manifests) or packages pinned (lockfiles).
   */
  entries: number;
  /**
   * Manifest or lockfile.
   */
  kind: "manifest" | "lockfile";
  /**
   * Explanation when parsing was partial or failed.
   */
  message?: string | null;
  /**
   * Declared package name.
   */
  packageName?: string | null;
  /**
   * Declared package version.
   */
  packageVersion?: string | null;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Parse outcome.
   */
  status: "parsed" | "partial" | "failed";
}
/**
 * A manifest whose dependency declarations have not changed for a long time.
 *
 * Without an advisory or registry provider RepoDNA cannot know whether newer versions exist;
 * this signal only reports how long the declarations have been unchanged.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "StaleSignal".
 */
export interface StaleSignal {
  /**
   * Days between that change and the latest commit in the repository.
   */
  daysUnchanged: number;
  /**
   * Neutral description.
   */
  description: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  lastChanged: string;
  /**
   * Manifest path.
   */
  manifest: string;
}
/**
 * Documentation.
 */
export interface DocsReport {
  /**
   * Documentation checklist.
   */
  checks: DocCheck[];
  /**
   * Purpose statement quoted from the README or a manifest, when found.
   */
  description?: string | null;
  /**
   * Where `description` was taken from.
   */
  descriptionSource?: string | null;
  /**
   * Directories dedicated to documentation.
   */
  docDirectories: string[];
  /**
   * Documentation files.
   */
  docFiles: number;
  /**
   * Lines in documentation files.
   */
  docLines: number;
  /**
   * Example files or directories.
   */
  examples: string[];
  /**
   * Detected license.
   */
  license?: LicenseInfo | null;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * README structure.
   */
  readme?: ReadmeInfo | null;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * One documentation check.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DocCheck".
 */
export interface DocCheck {
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Identifier, e.g. `contributing`.
   */
  id: string;
  /**
   * Label, e.g. `Contributing guide`.
   */
  label: string;
  /**
   * `true` for documents many projects legitimately omit.
   */
  optional: boolean;
  /**
   * Outcome.
   */
  status: "present" | "partial" | "not-detected";
}
/**
 * A detected license.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LicenseInfo".
 */
export interface LicenseInfo {
  /**
   * Confidence of the identification.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * File the license was detected in.
   */
  path: string;
  /**
   * SPDX identifier, when recognized.
   */
  spdx?: string | null;
}
/**
 * Structure of the README.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ReadmeInfo".
 */
export interface ReadmeInfo {
  /**
   * Fenced code blocks.
   */
  codeBlocks: number;
  /**
   * A heading mentions installation or setup.
   */
  hasInstallation: boolean;
  /**
   * A heading mentions usage, examples, or getting started.
   */
  hasUsage: boolean;
  /**
   * Headings in order (capped).
   */
  headings: string[];
  /**
   * Images.
   */
  images: number;
  /**
   * Lines.
   */
  lines: number;
  /**
   * Links.
   */
  links: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Words.
   */
  words: number;
}
/**
 * Evolution and Time Machine.
 */
export interface EvolutionReport {
  /**
   * Directories that stopped changing.
   */
  abandonedAreas: AbandonedArea[];
  archaeology: ArchaeologyReport;
  /**
   * Development epochs, oldest first.
   */
  epochs: Epoch[];
  /**
   * Historical events, oldest first.
   */
  events: EvolutionEvent[];
  /**
   * Growth curve derived from snapshots.
   */
  growth: GrowthPoint[];
  /**
   * Module ages.
   */
  moduleAges: ModuleAge[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * How snapshots were sampled.
   */
  sampling: string;
  /**
   * Time Machine snapshots, oldest first.
   */
  snapshots: Snapshot[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Chronological Project Story.
   */
  story: StoryStatement[];
}
/**
 * A directory that stopped changing while the rest of the repository continued.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AbandonedArea".
 */
export interface AbandonedArea {
  /**
   * Days between that change and the latest commit in the repository.
   */
  daysBeforeLatest: number;
  /**
   * Neutral description, e.g. "No changes detected since 2021-03-04."
   */
  description: string;
  /**
   * Files currently in the directory.
   */
  files: number;
  /**
   * Latest change inside the directory.
   */
  lastChanged: string;
  /**
   * Directory path.
   */
  path: string;
}
/**
 * Software-archaeology view.
 */
export interface ArchaeologyReport {
  /**
   * The current state.
   */
  currentState: StoryStatement[];
  /**
   * Major expansions.
   */
  expansions: StoryStatement[];
  /**
   * The final (most recent) active phase.
   */
  finalPhase: StoryStatement[];
  /**
   * How it grew.
   */
  growth: StoryStatement[];
  /**
   * Periods of inactivity.
   */
  inactivity: StoryStatement[];
  /**
   * How the project started.
   */
  origin: StoryStatement[];
  /**
   * Major rewrites and restructurings.
   */
  rewrites: StoryStatement[];
}
/**
 * A sentence of a narrative, linked to its evidence.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "StoryStatement".
 */
export interface StoryStatement {
  /**
   * Date the statement refers to.
   */
  date?: Timestamp | null;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Fact or interpretation.
   */
  kind: "fact" | "interpretation";
  /**
   * The sentence.
   */
  text: string;
}
/**
 * A contiguous period of development separated from others by long gaps or activity shifts.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Epoch".
 */
export interface Epoch {
  /**
   * Commits in the epoch.
   */
  commits: number;
  /**
   * Distinct authors in the epoch.
   */
  contributors: number;
  /**
   * Lines deleted.
   */
  deletions: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  end: string;
  /**
   * Directories that changed most during the epoch.
   */
  focusAreas: string[];
  /**
   * Position in the history (0 = first).
   */
  index: number;
  /**
   * Lines added.
   */
  insertions: number;
  /**
   * Epoch kind.
   */
  kind: "initial" | "active" | "maintenance" | "current";
  /**
   * Label, e.g. `Initial development`.
   */
  label: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  start: string;
}
/**
 * An evidence-backed historical event.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EvolutionEvent".
 */
export interface EvolutionEvent {
  /**
   * Commit most directly associated with the event.
   */
  commit?: string | null;
  /**
   * Confidence.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  date: string;
  /**
   * Description stating only what the evidence shows.
   */
  description: string;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Stable identifier.
   */
  id: string;
  /**
   * Event kind.
   */
  kind:
    | "repository-created"
    | "module-introduced"
    | "module-removed"
    | "restructuring"
    | "language-introduced"
    | "language-shift"
    | "framework-adopted"
    | "framework-removed"
    | "tests-introduced"
    | "ci-adopted"
    | "containers-adopted"
    | "significant-growth"
    | "significant-reduction"
    | "multi-package-structure"
    | "release";
  /**
   * Short title, e.g. "A new `network/` area appeared".
   */
  title: string;
}
/**
 * One point of the growth curve.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "GrowthPoint".
 */
export interface GrowthPoint {
  /**
   * Bytes.
   */
  bytes: number;
  /**
   * Commits so far.
   */
  commits: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  date: string;
  /**
   * Files.
   */
  files: number;
}
/**
 * Age of a module, with actual dates.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ModuleAge".
 */
export interface ModuleAge {
  /**
   * Days between the first commit and the latest repository commit.
   */
  ageDays: number;
  /**
   * Age class.
   */
  class: "ancient" | "established" | "growing" | "recent" | "new";
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  firstCommit: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  lastChange: string;
  /**
   * Module identifier.
   */
  module: string;
  /**
   * Why the class was assigned.
   */
  reason: string;
}
/**
 * The repository as it existed at one revision.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Snapshot".
 */
export interface Snapshot {
  /**
   * Module-level architecture, when historical parsing was enabled.
   */
  architecture?: SnapshotArchitecture | null;
  /**
   * Bytes.
   */
  bytes: number;
  /**
   * Commits up to and including this revision.
   */
  commitIndex: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  date: string;
  /**
   * Direct dependencies declared by manifests at this revision, when parsed.
   */
  dependencies?: number | null;
  /**
   * Top-level directories.
   */
  directories: SnapshotDirectory[];
  /**
   * Documentation files.
   */
  docFiles: number;
  /**
   * Files.
   */
  files: number;
  /**
   * Why the revision was chosen.
   */
  kind: "initial" | "release" | "sample" | "current";
  /**
   * Label, e.g. `Initial commit`, `v1.0.0`, `2023-05`, or `Current`.
   */
  label: string;
  /**
   * Language statistics.
   */
  languages: SnapshotLanguage[];
  /**
   * Commit hash.
   */
  revision: string;
  /**
   * Test files.
   */
  testFiles: number;
}
/**
 * Architecture at a snapshot (deep profile only).
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SnapshotArchitecture".
 */
export interface SnapshotArchitecture {
  /**
   * Module edges.
   */
  edges: SnapshotEdge[];
  /**
   * Modules present, with file counts.
   */
  modules: SnapshotDirectory[];
}
/**
 * A module edge in a historical architecture snapshot.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SnapshotEdge".
 */
export interface SnapshotEdge {
  /**
   * Dependent module.
   */
  from: string;
  /**
   * Dependency module.
   */
  to: string;
  /**
   * Number of supporting file edges.
   */
  weight: number;
}
/**
 * Size of a top-level directory at a snapshot.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SnapshotDirectory".
 */
export interface SnapshotDirectory {
  /**
   * Bytes.
   */
  bytes: number;
  /**
   * Files.
   */
  files: number;
  /**
   * Directory path (empty for files at the root).
   */
  path: string;
}
/**
 * Language statistics at a snapshot, measured in bytes because historical line counts
 * would require reading every historical file.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SnapshotLanguage".
 */
export interface SnapshotLanguage {
  /**
   * Bytes.
   */
  bytes: number;
  /**
   * Files.
   */
  files: number;
  /**
   * Language identifier.
   */
  id: string;
  /**
   * Share of bytes among languages that count toward composition (0–1).
   */
  share: number;
}
/**
 * An evidence-backed observation about the repository.
 *
 * Findings answer: *what is this* (`title`, `summary`), *why does it matter* (`rationale`),
 * *how was it calculated* (`method`), *what supports it* (`evidence`), *how sure are we*
 * (`confidence`, `limitations`), and *what to look at next* (`next_steps`).
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Finding".
 */
export interface Finding {
  /**
   * Dashboard category.
   */
  category:
    | "structure"
    | "architecture"
    | "dependencies"
    | "activity"
    | "contributors"
    | "complexity"
    | "duplication"
    | "maintainability"
    | "tests"
    | "build"
    | "documentation"
    | "security"
    | "evolution"
    | "plugin";
  /**
   * Confidence of the underlying method.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Facts that support the finding.
   */
  evidence: Evidence[];
  /**
   * Stable identifier: `<rule>:<hash of rule and subject>`. Stays the same across scans
   * while the underlying subject (file, cycle, package…) is unchanged.
   */
  id: string;
  /**
   * Known limitations of the method for this finding.
   */
  limitations?: string[];
  /**
   * How the observation was calculated.
   */
  method: string;
  /**
   * Suggested next steps for investigation.
   */
  nextSteps?: string[];
  /**
   * Repository paths the finding is about; used for linking and suppression matching.
   */
  paths?: string[];
  /**
   * Why the observation may matter.
   */
  rationale: string;
  /**
   * Rule identifier, e.g. `architecture.cycle`.
   */
  rule: string;
  /**
   * Severity assigned according to the documented criteria.
   */
  severity: "info" | "attention" | "warning" | "critical";
  /**
   * What was observed.
   */
  summary: string;
  /**
   * Present when a suppression rule matched this finding.
   */
  suppressed?: Suppression | null;
  /**
   * Short title, e.g. `Dependency cycle`.
   */
  title: string;
}
/**
 * Records that a finding matched a user-defined suppression rule.
 *
 * Suppressed findings stay in the artifact so suppression is always visible and auditable.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Suppression".
 */
export interface Suppression {
  /**
   * The reason recorded in the suppression rule.
   */
  reason: string;
  /**
   * Where the rule was defined, e.g. `repodna.toml` or `user configuration`.
   */
  source: string;
}
/**
 * Project DNA fingerprint.
 */
export interface DnaFingerprint {
  /**
   * Characterization dimensions.
   */
  dimensions: DnaDimension[];
  /**
   * Deterministic snapshot identifier: `rdna1-` followed by 32 hexadecimal characters.
   * It identifies content; it is not a security primitive.
   */
  dnaHash: string;
  /**
   * How the hash and dimensions were computed.
   */
  method: string;
}
/**
 * One dimension of the DNA fingerprint.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DnaDimension".
 */
export interface DnaDimension {
  /**
   * Confidence (`unavailable` when the underlying analysis did not run).
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * What the dimension describes and how it was normalized.
   */
  description: string;
  /**
   * Identifier, e.g. `language-diversity`.
   */
  id: string;
  /**
   * Label, e.g. `Language diversity`.
   */
  label: string;
  /**
   * Raw measurement before normalization.
   */
  raw: number;
  /**
   * Unit of `raw`.
   */
  unit: string;
  /**
   * Normalized value (0–1). Higher is not "better"; it only positions the repository.
   */
  value: number;
}
/**
 * A report generated from this artifact.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "GeneratedReport".
 */
export interface GeneratedReport {
  /**
   * Format, e.g. `html`.
   */
  format: string;
  /**
   * Path relative to the report output directory.
   */
  path: string;
}
/**
 * Git history.
 */
export interface GitReport {
  activity: ActivitySummary;
  /**
   * Branches.
   */
  branches: BranchInfo[];
  /**
   * Number of commits analyzed (reachable from HEAD).
   */
  commitCount: number;
  /**
   * Most recent commits (capped by configuration).
   */
  commits: CommitRecord[];
  /**
   * Contributors sorted by commits, descending.
   */
  contributors: ContributorRecord[];
  /**
   * Branch checked out at analysis time.
   */
  currentBranch?: string | null;
  /**
   * Daily commit activity (days with at least one commit).
   */
  dailyActivity: DailyActivity[];
  /**
   * Default branch, when it can be determined.
   */
  defaultBranch?: string | null;
  /**
   * History per directory.
   */
  directoryActivity: DirectoryActivity[];
  /**
   * Gaps without commits longer than the configured threshold.
   */
  dormantPeriods: DormantPeriod[];
  /**
   * History per current file, sorted by path.
   */
  fileHistory: FileHistoryRecord[];
  /**
   * Earliest analyzed commit.
   */
  firstCommit?: CommitRef | null;
  /**
   * Git version used for the analysis.
   */
  gitVersion?: string | null;
  /**
   * Commit analyzed.
   */
  head?: CommitRef | null;
  /**
   * `true` when the configured commit limit was reached.
   */
  historyTruncated?: boolean;
  /**
   * Ranked hotspots.
   */
  hotSpots: Hotspot[];
  /**
   * Latest analyzed commit.
   */
  lastCommit?: CommitRef | null;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  ownership: OwnershipSummary;
  /**
   * Size of the "recent" window used for recency signals, in days before the latest commit.
   */
  recentWindowDays: number;
  /**
   * Tags that look like release versions, oldest first.
   */
  releases: ReleaseInfo[];
  /**
   * `true` when the clone is shallow and history is incomplete.
   */
  shallow?: boolean;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Tags.
   */
  tags: TagInfo[];
  /**
   * Monthly commit activity.
   */
  timeline: TimelineBucket[];
  /**
   * Commits per weekday (0 = Monday) and hour (0–23), in the author's local time.
   */
  weekdayHour: number[][];
}
/**
 * Activity summary.
 */
export interface ActivitySummary {
  /**
   * Commits in the 30 days before the reference time.
   */
  commitsLast30Days: number;
  /**
   * Commits in the 365 days before the reference time.
   */
  commitsLast365Days: number;
  /**
   * Commits in the 90 days before the reference time.
   */
  commitsLast90Days: number;
  /**
   * Days between the latest commit and the reference time.
   */
  daysSinceLastCommit?: number | null;
  /**
   * Neutral description, e.g. "No activity detected after 2021-03-04."
   */
  description: string;
  /**
   * Activity level according to the documented thresholds.
   */
  level: "very-active" | "active" | "moderate" | "low" | "dormant" | "none";
  /**
   * Time the activity was measured against (normally the analysis time).
   */
  referenceTime?: Timestamp | null;
}
/**
 * A branch.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "BranchInfo".
 */
export interface BranchInfo {
  /**
   * Commit the branch points to.
   */
  head: string;
  /**
   * Branch name, e.g. `main` or `origin/main`.
   */
  name: string;
  /**
   * `true` for remote-tracking branches.
   */
  remote: boolean;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  updated: string;
}
/**
 * A commit with its change statistics.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CommitRecord".
 */
export interface CommitRecord {
  /**
   * Identifier of the author (see [`ContributorRecord::id`]).
   */
  author: string;
  /**
   * Lines deleted.
   */
  deletions: number;
  /**
   * Files changed (zero for merge commits, which are not diffed).
   */
  filesChanged: number;
  /**
   * Full commit hash.
   */
  hash: string;
  /**
   * Lines added.
   */
  insertions: number;
  /**
   * `true` for merge commits.
   */
  merge?: boolean;
  /**
   * Abbreviated hash (first 7 characters).
   */
  short: string;
  /**
   * Subject line (may be removed by privacy settings).
   */
  subject: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  timestamp: string;
}
/**
 * A contributor as recorded in public Git history.
 *
 * Contribution statistics describe the history; they are not judgments about people.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ContributorRecord".
 */
export interface ContributorRecord {
  /**
   * Distinct days with at least one commit.
   */
  activeDays: number;
  /**
   * Directories most often touched.
   */
  areas: AreaShare[];
  /**
   * Number of commits.
   */
  commits: number;
  /**
   * Lines deleted.
   */
  deletions: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  firstCommit: string;
  /**
   * Stable pseudonymous identifier derived from the normalized e-mail address.
   * E-mail addresses themselves are never stored.
   */
  id: string;
  /**
   * Lines added.
   */
  insertions: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  lastCommit: string;
  /**
   * Name as recorded in commits (after `.mailmap`), or a pseudonym when anonymized.
   */
  name: string;
}
/**
 * Share of a contributor's commits within one directory.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AreaShare".
 */
export interface AreaShare {
  /**
   * Commits touching the directory.
   */
  commits: number;
  /**
   * Directory path (top two levels at most).
   */
  path: string;
}
/**
 * Commit activity for one day.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DailyActivity".
 */
export interface DailyActivity {
  /**
   * Lines added plus deleted.
   */
  churn: number;
  /**
   * Commits on that day.
   */
  commits: number;
  /**
   * Date, `YYYY-MM-DD` (UTC).
   */
  date: string;
}
/**
 * History statistics for a directory.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DirectoryActivity".
 */
export interface DirectoryActivity {
  /**
   * Distinct authors.
   */
  authors: number;
  /**
   * Lines added plus deleted.
   */
  churn: number;
  /**
   * Commits touching the directory.
   */
  commits: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  lastChanged: string;
  /**
   * Directory path (top two levels at most).
   */
  path: string;
  /**
   * Share of the repository's churn in the recent window (0–1).
   */
  recentChurnShare: number;
}
/**
 * A period without commits.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DormantPeriod".
 */
export interface DormantPeriod {
  /**
   * Commit after the gap.
   */
  afterCommit: string;
  /**
   * Commit before the gap.
   */
  beforeCommit: string;
  /**
   * Length of the gap in days.
   */
  days: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  end: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  start: string;
}
/**
 * History statistics for one file.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileHistoryRecord".
 */
export interface FileHistoryRecord {
  /**
   * Distinct authors.
   */
  authors: number;
  /**
   * Commits that changed the file.
   */
  commits: number;
  /**
   * Lines deleted across history.
   */
  deletions: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  firstSeen: string;
  /**
   * Lines added across history.
   */
  insertions: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  lastChanged: string;
  /**
   * Current repository-relative path.
   */
  path: string;
  /**
   * Earlier paths of the file, when renames were detected.
   */
  previousPaths?: string[];
  /**
   * Commits in the recent window (see `GitReport::recentWindowDays`).
   */
  recentCommits: number;
}
/**
 * A reference to a commit.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CommitRef".
 */
export interface CommitRef {
  /**
   * Full commit hash.
   */
  hash: string;
  /**
   * Abbreviated hash (first 7 characters).
   */
  short: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  timestamp: string;
}
/**
 * A file that changes frequently and carries other risk-relevant signals.
 *
 * A hotspot is a prioritization aid, not a defect claim.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Hotspot".
 */
export interface Hotspot {
  /**
   * Distinct authors.
   */
  authors: number;
  /**
   * Lines added plus deleted.
   */
  churn: number;
  /**
   * Commits touching the file.
   */
  commits: number;
  /**
   * Highest function complexity in the file.
   */
  complexity: number;
  /**
   * Number of files that import this file.
   */
  dependents: number;
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Neutral interpretation of what the signals mean.
   */
  interpretation: string;
  /**
   * Code lines.
   */
  lines: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Rank (1 = strongest hotspot).
   */
  rank: number;
  /**
   * Why the file is a hotspot, one reason per signal.
   */
  reasons: string[];
  /**
   * Commits in the recent window.
   */
  recentCommits: number;
  /**
   * Combined score (0–1); see `method` in the documentation.
   */
  score: number;
}
/**
 * Commit concentration among contributors.
 */
export interface OwnershipSummary {
  /**
   * Number of contributors.
   */
  contributors: number;
  /**
   * Fewest contributors who together authored at least half of all commits.
   */
  contributorsForHalfOfCommits: number;
  /**
   * Neutral explanation of the numbers.
   */
  note: string;
  /**
   * Share of commits by the most active contributor (0–1).
   */
  topContributorShare: number;
}
/**
 * A tag whose name looks like a release version.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ReleaseInfo".
 */
export interface ReleaseInfo {
  /**
   * Commit the release points to.
   */
  commit: string;
  /**
   * Commits between the previous release and this one.
   */
  commitsSincePrevious: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  date: string;
  /**
   * Tag name, e.g. `v1.2.0`.
   */
  tag: string;
  /**
   * Parsed version string without prefix, e.g. `1.2.0`.
   */
  version: string;
}
/**
 * A tag.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "TagInfo".
 */
export interface TagInfo {
  /**
   * `true` for annotated tags.
   */
  annotated: boolean;
  /**
   * Commit the tag points to (peeled).
   */
  commit: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  date: string;
  /**
   * Tag name.
   */
  name: string;
}
/**
 * Commit activity for one calendar month.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "TimelineBucket".
 */
export interface TimelineBucket {
  /**
   * Distinct authors in the month.
   */
  authors: number;
  /**
   * Commits in the month.
   */
  commits: number;
  /**
   * Lines deleted.
   */
  deletions: number;
  /**
   * Lines added.
   */
  insertions: number;
  /**
   * Month key, `YYYY-MM`.
   */
  period: string;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  start: string;
}
/**
 * Repository identity.
 */
export interface RepositoryIdentity {
  /**
   * Default branch, when known.
   */
  defaultBranch?: string | null;
  /**
   * Purpose statement quoted from the README or a manifest.
   */
  description?: string | null;
  /**
   * `true` when Git metadata was available.
   */
  isGitRepository: boolean;
  /**
   * Detected license.
   */
  license?: LicenseInfo | null;
  /**
   * Repository name (from the remote URL or the directory name).
   */
  name: string;
  /**
   * Owner or namespace from the remote URL, when available.
   */
  owner?: string | null;
  /**
   * Primary language identifiers.
   */
  primaryLanguages: string[];
  /**
   * Remotes with credentials removed.
   */
  remotes: RemoteInfo[];
  /**
   * Commit analyzed, when the repository uses Git.
   */
  revision?: string | null;
  /**
   * Size class by file count.
   */
  sizeClass: "tiny" | "small" | "medium" | "large" | "very-large";
}
/**
 * A Git remote with credentials removed.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "RemoteInfo".
 */
export interface RemoteInfo {
  /**
   * Host name, e.g. `github.com`.
   */
  host?: string | null;
  /**
   * Remote name, e.g. `origin`.
   */
  name: string;
  /**
   * Hosting provider inferred from the host: `github`, `gitlab`, `bitbucket`, or `other`.
   */
  provider: string;
  /**
   * URL with any user name, password, or token removed.
   */
  url: string;
}
/**
 * First-look answers, onboarding, and recent changes.
 */
export interface Insights {
  /**
   * Answers to the "first look" questions.
   */
  firstLook: QuestionAnswer[];
  /**
   * Repository-specific terminology.
   */
  glossary: GlossaryTerm[];
  /**
   * Files worth reading first.
   */
  importantFiles: ImportantFile[];
  /**
   * Suggested onboarding path.
   */
  onboarding: GuideStep[];
  /**
   * What changed recently.
   */
  recentChanges?: RecentChanges | null;
}
/**
 * An answer to one "first look" question such as "Where does it start?".
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "QuestionAnswer".
 */
export interface QuestionAnswer {
  /**
   * The answer, written from the evidence.
   */
  answer: string;
  /**
   * Confidence of the answer.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Stable identifier, e.g. `entrypoints`.
   */
  id: string;
  /**
   * Fact or interpretation.
   */
  kind: "fact" | "interpretation";
  /**
   * The question.
   */
  question: string;
}
/**
 * A term used in the repository, with a definition derived from the analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "GlossaryTerm".
 */
export interface GlossaryTerm {
  /**
   * What the term refers to in this repository.
   */
  definition: string;
  /**
   * The term, e.g. a module or tool name.
   */
  term: string;
}
/**
 * A file worth reading early, with the reasons it ranks highly.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ImportantFile".
 */
export interface ImportantFile {
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Evidence-backed reasons.
   */
  reasons: string[];
  /**
   * Ranking score (0–1).
   */
  score: number;
}
/**
 * One step of an onboarding or learning path.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "GuideStep".
 */
export interface GuideStep {
  /**
   * Commands referenced by the step (detected, not verified).
   */
  commands: string[];
  /**
   * What to do and why.
   */
  description: string;
  /**
   * Repository files referenced by the step.
   */
  paths: string[];
  /**
   * Step title, e.g. `Read the entrypoint`.
   */
  title: string;
}
/**
 * What changed during the recent window before the analyzed revision.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "RecentChanges".
 */
export interface RecentChanges {
  /**
   * Files added in the window that still exist.
   */
  addedFiles: string[];
  /**
   * Commits in the window.
   */
  commits: number;
  /**
   * Distinct authors in the window.
   */
  contributors: number;
  /**
   * Lines deleted.
   */
  deletions: number;
  /**
   * Dependency declaration changes in the window.
   */
  dependencyChanges: DependencyChange[];
  /**
   * Directories with the most churn.
   */
  directories: DirectoryChange[];
  /**
   * Documentation files changed in the window.
   */
  docFilesChanged: number;
  /**
   * Distinct files changed.
   */
  filesChanged: number;
  /**
   * Lines added.
   */
  insertions: number;
  /**
   * Manifests changed in the window.
   */
  manifestsChanged: string[];
  /**
   * Files deleted in the window.
   */
  removedFiles: string[];
  /**
   * Start of the window.
   */
  since?: Timestamp | null;
  /**
   * Test files changed in the window.
   */
  testFilesChanged: number;
  /**
   * End of the window (the latest commit).
   */
  until?: Timestamp | null;
  /**
   * Window size in days, ending at the latest commit.
   */
  windowDays: number;
}
/**
 * A dependency declaration change between two revisions.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyChange".
 */
export interface DependencyChange {
  /**
   * Kind of change.
   */
  change: "added" | "removed" | "changed";
  /**
   * Ecosystem identifier.
   */
  ecosystem: string;
  /**
   * Previous requirement.
   */
  from?: string | null;
  /**
   * Manifest where the change happened.
   */
  manifest: string;
  /**
   * Package name.
   */
  name: string;
  /**
   * New requirement.
   */
  to?: string | null;
}
/**
 * Recent change activity in one directory.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DirectoryChange".
 */
export interface DirectoryChange {
  /**
   * Lines added plus deleted in the window.
   */
  churn: number;
  /**
   * Commits touching it in the window.
   */
  commits: number;
  /**
   * Directory path.
   */
  path: string;
}
/**
 * Language composition.
 */
export interface LanguageReport {
  /**
   * Cross-language relationships.
   */
  interactions: LanguageInteraction[];
  /**
   * Languages sorted by code lines, descending.
   */
  languages: LanguageStat[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Identifiers of the primary languages (up to three, by share).
   */
  primary: string[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Text files whose language was not recognized.
   */
  unrecognizedFiles: number;
}
/**
 * A detected relationship between two languages in the same repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LanguageInteraction".
 */
export interface LanguageInteraction {
  /**
   * Confidence of the detection.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Number of occurrences observed.
   */
  count: number;
  /**
   * Plain-language description.
   */
  description: string;
  /**
   * Sample evidence (capped).
   */
  evidence: Evidence[];
  /**
   * Language that depends on or calls into the other.
   */
  from: string;
  /**
   * Mechanism, e.g. `import`, `file-reference`, `ffi`, or `tauri-command`.
   */
  mechanism: string;
  /**
   * Language being referenced.
   */
  to: string;
}
/**
 * Statistics for one language.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LanguageStat".
 */
export interface LanguageStat {
  /**
   * Blank lines.
   */
  blankLines: number;
  /**
   * Total bytes.
   */
  bytes: number;
  /**
   * Depth of analysis available for this language.
   */
  capability: "detection" | "line-count" | "lexical" | "syntactic" | "semantic";
  /**
   * Code lines.
   */
  codeLines: number;
  /**
   * Comment-only lines.
   */
  commentLines: number;
  /**
   * Number of files.
   */
  files: number;
  /**
   * Stable identifier, e.g. `typescript`.
   */
  id: string;
  /**
   * Language family.
   */
  kind: "programming" | "markup" | "stylesheet" | "data" | "prose" | "build";
  /**
   * Display name, e.g. `TypeScript`.
   */
  name: string;
  /**
   * Share of code lines among languages that count toward composition (0–1).
   */
  share: number;
}
/**
 * Metrics, normalized signals, and confidence.
 */
export interface MetricsReport {
  /**
   * Confidence per section.
   */
  confidence: SectionConfidence[];
  /**
   * Raw metrics, sorted by identifier.
   */
  raw: Metric[];
  /**
   * Normalized signals.
   */
  signals: NormalizedSignal[];
}
/**
 * Confidence of an analysis section and the reason for it.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SectionConfidence".
 */
export interface SectionConfidence {
  /**
   * Confidence.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Why the confidence was assigned.
   */
  reason: string;
  /**
   * Section identifier, e.g. `architecture`.
   */
  section: string;
}
/**
 * A measured value, documented well enough that a reader can reproduce it.
 *
 * Every metric answers: what is measured (`definition`), how (`method`), the current value,
 * the configured threshold if any, and the confidence and limitations of the measurement.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Metric".
 */
export interface Metric {
  /**
   * Confidence of the measurement method.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * What the metric measures.
   */
  definition: string;
  /**
   * Identifier, e.g. `structure.files.total`.
   */
  id: string;
  /**
   * Short human-readable label.
   */
  label: string;
  /**
   * Known limitations of the measurement.
   */
  limitations?: string[];
  /**
   * How the value was calculated.
   */
  method: string;
  /**
   * Threshold used for signals derived from this metric, if any.
   */
  threshold?: number | null;
  /**
   * Unit of the value, e.g. `files`, `lines`, `%`, `commits`, or `ratio`.
   */
  unit: string;
  /**
   * Measured value.
   */
  value: number;
}
/**
 * A metric normalized to 0–1 for visualization, with its interpretation.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "NormalizedSignal".
 */
export interface NormalizedSignal {
  /**
   * Confidence.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Identifier.
   */
  id: string;
  /**
   * Neutral interpretation of the value.
   */
  interpretation: string;
  /**
   * Label.
   */
  label: string;
  /**
   * Normalized value (0–1).
   */
  value: number;
}
/**
 * Result of running one plugin.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PluginRunRecord".
 */
export interface PluginRunRecord {
  /**
   * Wall-clock duration in milliseconds.
   */
  durationMs: number;
  /**
   * Findings contributed.
   */
  findings: number;
  /**
   * Explanation for failures.
   */
  message?: string | null;
  /**
   * Metrics contributed.
   */
  metrics: number;
  /**
   * Plugin name.
   */
  name: string;
  /**
   * Outcome.
   */
  status: "completed" | "partial" | "skipped" | "failed" | "cancelled";
  /**
   * Plugin version.
   */
  version: string;
}
/**
 * Security signals.
 */
export interface SecurityReport {
  advisories: AdvisoryStatus1;
  /**
   * What these signals do and do not mean.
   */
  disclaimer: string;
  /**
   * Text files scanned.
   */
  filesScanned: number;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Potentially dangerous patterns.
   */
  patterns: PatternCandidate[];
  /**
   * Unusual file permissions.
   */
  permissions: PermissionSignal[];
  /**
   * Rules applied.
   */
  rules: SecurityRuleInfo[];
  /**
   * Possible credentials (values redacted).
   */
  secrets: SecretCandidate[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Advisory integration status.
 */
export interface AdvisoryStatus1 {
  /**
   * When the advisory data was retrieved.
   */
  checkedAt?: Timestamp | null;
  /**
   * Explanation, e.g. that no provider was configured.
   */
  note: string;
  /**
   * Advisory provider used, if any.
   */
  provider?: string | null;
}
/**
 * A potentially dangerous pattern.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PatternCandidate".
 */
export interface PatternCandidate {
  /**
   * Pattern category.
   */
  category: "code" | "configuration" | "workflow" | "container";
  /**
   * Confidence that the match is the risky construct.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * What was detected.
   */
  description: string;
  /**
   * Stable identifier.
   */
  id: string;
  /**
   * Line (1-based), when the pattern is line-based.
   */
  line?: number | null;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Suggested review action.
   */
  recommendation: string;
  /**
   * Rule identifier, e.g. `tls-verification-disabled`.
   */
  rule: string;
}
/**
 * A file permission that may be unsafe (reported on Unix-like systems only).
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PermissionSignal".
 */
export interface PermissionSignal {
  /**
   * What is unusual about the permissions.
   */
  issue: string;
  /**
   * Permission bits in octal, e.g. `0777`.
   */
  mode: string;
  /**
   * Repository-relative path.
   */
  path: string;
}
/**
 * A rule used by the security scanner.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SecurityRuleInfo".
 */
export interface SecurityRuleInfo {
  /**
   * What the rule detects.
   */
  description: string;
  /**
   * Rule identifier.
   */
  id: string;
  /**
   * `secret` or `pattern`.
   */
  kind: string;
}
/**
 * A location that looks like it contains a credential.
 *
 * Only the location and a keyed fingerprint are stored; the value itself is never written
 * to artifacts, reports, logs, or telemetry.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SecretCandidate".
 */
export interface SecretCandidate {
  /**
   * Confidence that the match is a real credential.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * What the rule detects.
   */
  description: string;
  /**
   * Keyed fingerprint that identifies identical values within this scan without
   * revealing them.
   */
  fingerprint: string;
  /**
   * Stable identifier.
   */
  id: string;
  /**
   * `true` when the path looks like tests, fixtures, examples, or documentation.
   */
  inTestOrExample?: boolean;
  /**
   * Line (1-based).
   */
  line: number;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Rule that matched, e.g. `private-key`.
   */
  rule: string;
}
/**
 * File similarity.
 */
export interface SimilarityReport {
  /**
   * How similarity was estimated.
   */
  method: string;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * File pairs above the similarity threshold, most similar first.
   */
  similarFiles: SimilarFilePair[];
  /**
   * Whether similarity analysis ran.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Threshold used (0–1).
   */
  threshold: number;
}
/**
 * A pair of files with similar content.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SimilarFilePair".
 */
export interface SimilarFilePair {
  /**
   * First file.
   */
  a: string;
  /**
   * Second file.
   */
  b: string;
  /**
   * Estimated Jaccard similarity of token shingles (0–1).
   */
  similarity: number;
}
/**
 * Files, directories, symbols, and entrypoints.
 */
export interface StructureReport {
  /**
   * Number of binary files.
   */
  binaryFiles: number;
  /**
   * Blank lines across text files.
   */
  blankLines: number;
  /**
   * File counts per category.
   */
  categories: CategoryCount[];
  /**
   * Code lines across text files.
   */
  codeLines: number;
  /**
   * Comment-only lines across text files.
   */
  commentLines: number;
  /**
   * Aggregated directory statistics, sorted by path.
   */
  directories: DirectoryRecord[];
  /**
   * Detected entrypoints.
   */
  entrypoints: Entrypoint[];
  /**
   * Every discovered file, sorted by path.
   */
  files: FileRecord[];
  /**
   * Number of files flagged as generated.
   */
  generatedFiles: number;
  /**
   * Ignore patterns applied in addition to `.gitignore` rules.
   */
  ignorePatterns?: string[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Size class by file count.
   */
  sizeClass: "tiny" | "small" | "medium" | "large" | "very-large";
  /**
   * Number of files whose content analysis was skipped.
   */
  skippedFiles: number;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Detected symbols (capped by configuration; see `truncated`).
   */
  symbols: SymbolRecord[];
  /**
   * Number of symbolic links encountered (never followed).
   */
  symlinks: number;
  /**
   * Total size of discovered files in bytes.
   */
  totalBytes: number;
  /**
   * Number of files discovered (after ignore rules).
   */
  totalFiles: number;
  /**
   * Total lines across text files.
   */
  totalLines: number;
  /**
   * `true` when lists in this section were capped to keep the artifact manageable.
   */
  truncated?: boolean;
  /**
   * Number of files flagged as vendored.
   */
  vendoredFiles: number;
}
/**
 * File counts for one category.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "CategoryCount".
 */
export interface CategoryCount {
  /**
   * Total bytes.
   */
  bytes: number;
  /**
   * The category.
   */
  category:
    | "source"
    | "test"
    | "documentation"
    | "configuration"
    | "ci-cd"
    | "build"
    | "manifest"
    | "lockfile"
    | "generated"
    | "asset"
    | "binary"
    | "vendor"
    | "build-output"
    | "ide-metadata"
    | "container"
    | "infrastructure"
    | "data"
    | "license"
    | "other";
  /**
   * Number of files.
   */
  files: number;
  /**
   * Total lines (text files only).
   */
  lines: number;
}
/**
 * Aggregated statistics for a directory, including all nested files.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DirectoryRecord".
 */
export interface DirectoryRecord {
  /**
   * Bytes contained, recursively.
   */
  bytes: number;
  /**
   * Code lines contained, recursively.
   */
  codeLines: number;
  /**
   * Number of path components (0 for the root).
   */
  depth: number;
  /**
   * Files contained, recursively.
   */
  files: number;
  /**
   * Repository-relative path of the directory (empty for the root).
   */
  path: string;
  /**
   * Language with the most code lines in the directory.
   */
  primaryLanguage?: string | null;
}
/**
 * A place where execution or consumption of the code starts.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Entrypoint".
 */
export interface Entrypoint {
  /**
   * Confidence of the identification.
   */
  confidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Supporting evidence.
   */
  evidence: Evidence[];
  /**
   * Entrypoint kind.
   */
  kind: "binary" | "library" | "web" | "cli" | "server" | "script";
  /**
   * Language of the entrypoint file.
   */
  language?: string | null;
  /**
   * Repository-relative path.
   */
  path: string;
  /**
   * Why the file was identified as an entrypoint.
   */
  reason: string;
}
/**
 * A file discovered in the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileRecord".
 */
export interface FileRecord {
  /**
   * Lexical analysis summary, absent when the language has no lexical analyzer.
   */
  analysis?: FileAnalysisSummary | null;
  /**
   * The file contains binary data.
   */
  binary?: boolean;
  /**
   * Size in bytes.
   */
  bytes: number;
  /**
   * Primary classification.
   */
  category:
    | "source"
    | "test"
    | "documentation"
    | "configuration"
    | "ci-cd"
    | "build"
    | "manifest"
    | "lockfile"
    | "generated"
    | "asset"
    | "binary"
    | "vendor"
    | "build-output"
    | "ide-metadata"
    | "container"
    | "infrastructure"
    | "data"
    | "license"
    | "other";
  /**
   * The file appears to be machine-generated.
   */
  generated?: boolean;
  /**
   * Short content hash (first 64 bits of SHA-256, hexadecimal).
   */
  hash?: string | null;
  /**
   * Detected language identifier, e.g. `rust`.
   */
  language?: string | null;
  /**
   * Line counts, absent for binary or skipped files.
   */
  lines?: LineCounts | null;
  /**
   * Identifier of the module this file belongs to.
   */
  module?: string | null;
  /**
   * Repository-relative path using forward slashes.
   */
  path: string;
  /**
   * Set when content analysis was skipped.
   */
  skipped?: SkipReason | null;
  /**
   * The file is third-party code vendored into the repository.
   */
  vendored?: boolean;
}
/**
 * Per-file results of lexical analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "FileAnalysisSummary".
 */
export interface FileAnalysisSummary {
  /**
   * Highest approximate cyclomatic complexity of any function.
   */
  cyclomaticMax: number;
  /**
   * Sum of the approximate cyclomatic complexity of all functions.
   */
  cyclomaticTotal: number;
  /**
   * Functions and methods detected.
   */
  functions: number;
  /**
   * Import, include, or require statements detected.
   */
  imports: number;
  /**
   * Length in lines of the longest function.
   */
  longestFunction: number;
  /**
   * TODO/FIXME-style markers found in comments.
   */
  markers: number;
  /**
   * Deepest block nesting observed.
   */
  maxNesting: number;
  /**
   * Symbols (functions, types, …) detected.
   */
  symbols: number;
}
/**
 * Line counts for a text file.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LineCounts".
 */
export interface LineCounts {
  /**
   * Lines containing only whitespace.
   */
  blank: number;
  /**
   * Lines containing code (a line with code and a trailing comment counts as code).
   */
  code: number;
  /**
   * Lines containing only comments.
   */
  comment: number;
  /**
   * All lines.
   */
  total: number;
}
/**
 * A symbol detected by lexical analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SymbolRecord".
 */
export interface SymbolRecord {
  /**
   * Approximate cyclomatic complexity, for functions and methods.
   */
  complexity?: number | null;
  /**
   * Last line of the definition, when known.
   */
  endLine?: number | null;
  /**
   * Symbol kind.
   */
  kind:
    | "function"
    | "method"
    | "class"
    | "struct"
    | "enum"
    | "interface"
    | "trait"
    | "module"
    | "type"
    | "constant"
    | "macro"
    | "schema";
  /**
   * Line of the definition (1-based).
   */
  line: number;
  /**
   * Symbol name.
   */
  name: string;
  /**
   * Repository-relative path of the defining file.
   */
  path: string;
}
/**
 * Test detection.
 */
export interface TestReport {
  /**
   * Test commands found in CI configuration.
   */
  ciCommands: CommandCandidate[];
  /**
   * Test commands derived from metadata.
   */
  commands: CommandCandidate[];
  /**
   * Coverage reports present in the repository.
   */
  coverageArtifacts: string[];
  /**
   * Results of explicitly authorized test runs.
   */
  executions: ExecutionResult[];
  /**
   * Detected test frameworks.
   */
  frameworks: DetectedTool[];
  /**
   * Source files that also contain tests, such as Rust `#[cfg(test)]` modules. Their
   * code is counted as source code.
   */
  inlineTestFiles: number;
  /**
   * Kinds of tests detected.
   */
  kinds: TestKindSignal[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Files classified as first-party source code.
   */
  sourceFiles: number;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Directories that contain tests.
   */
  testDirectories: string[];
  /**
   * Files classified as tests.
   */
  testFiles: number;
  /**
   * Code lines in test files.
   */
  testLines: number;
  /**
   * `testLines / (testLines + sourceCodeLines)` (0–1).
   */
  testRatio: number;
}
/**
 * Evidence for one kind of tests.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "TestKindSignal".
 */
export interface TestKindSignal {
  /**
   * Sample evidence.
   */
  evidence: Evidence[];
  /**
   * Files suggesting this kind.
   */
  files: number;
  /**
   * Test kind.
   */
  kind: "unit" | "integration" | "end-to-end" | "snapshot" | "benchmark";
}
/**
 * The tool that produced the artifact.
 */
export interface ToolInfo {
  /**
   * Always `RepoDNA`.
   */
  name: string;
  /**
   * Artifact schema version, e.g. `1.0`.
   */
  schemaVersion: string;
  /**
   * Tool version, e.g. `1.0.0`.
   */
  version: string;
}
/**
 * Activity summary relative to a reference time.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ActivitySummary".
 */
export interface ActivitySummary1 {
  /**
   * Commits in the 30 days before the reference time.
   */
  commitsLast30Days: number;
  /**
   * Commits in the 365 days before the reference time.
   */
  commitsLast365Days: number;
  /**
   * Commits in the 90 days before the reference time.
   */
  commitsLast90Days: number;
  /**
   * Days between the latest commit and the reference time.
   */
  daysSinceLastCommit?: number | null;
  /**
   * Neutral description, e.g. "No activity detected after 2021-03-04."
   */
  description: string;
  /**
   * Activity level according to the documented thresholds.
   */
  level: "very-active" | "active" | "moderate" | "low" | "dormant" | "none";
  /**
   * Time the activity was measured against (normally the analysis time).
   */
  referenceTime?: Timestamp | null;
}
/**
 * Status of vulnerability-advisory integration.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AdvisoryStatus".
 */
export interface AdvisoryStatus2 {
  /**
   * When the advisory data was retrieved.
   */
  checkedAt?: Timestamp | null;
  /**
   * Explanation, e.g. that no provider was configured.
   */
  note: string;
  /**
   * Advisory provider used, if any.
   */
  provider?: string | null;
}
/**
 * Everything needed to understand how an artifact was produced.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "AnalysisMetadata".
 */
export interface AnalysisMetadata1 {
  /**
   * AI usage, when an AI provider was used.
   */
  ai?: AiUsage | null;
  /**
   * Per-stage outcomes and timings.
   */
  analyzers: AnalyzerRun[];
  /**
   * Hash of the effective configuration.
   */
  configHash: string;
  /**
   * Configuration layers that contributed.
   */
  configSources: string[];
  /**
   * Data sources used.
   */
  dataSources: DataSource[];
  /**
   * `true` when the working tree had uncommitted changes.
   */
  dirty?: boolean | null;
  /**
   * Total wall-clock duration in milliseconds.
   */
  durationMs: number;
  /**
   * An RFC 3339 date-time in UTC, e.g. 2026-09-24T10:30:00Z.
   */
  generatedAt: string;
  /**
   * Identifier of this analysis run.
   */
  id: string;
  input: InputInfo;
  /**
   * `true` when the analysis was cancelled or a stage failed.
   */
  partial: boolean;
  platform: PlatformInfo;
  privacy: PrivacyInfo;
  /**
   * Profile used.
   */
  profile:
    | "quick"
    | "standard"
    | "deep"
    | "history-only"
    | "architecture-only"
    | "dependencies-only"
    | "security-only";
  /**
   * Commit analyzed, when available.
   */
  revision?: string | null;
  /**
   * Findings suppressed by those rules.
   */
  suppressedFindings: number;
  /**
   * Suppression rules in effect.
   */
  suppressions: SuppressionRule[];
  thresholds: Thresholds;
  /**
   * Warnings raised during the analysis.
   */
  warnings: string[];
}
/**
 * Software-archaeology view of the history.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ArchaeologyReport".
 */
export interface ArchaeologyReport1 {
  /**
   * The current state.
   */
  currentState: StoryStatement[];
  /**
   * Major expansions.
   */
  expansions: StoryStatement[];
  /**
   * The final (most recent) active phase.
   */
  finalPhase: StoryStatement[];
  /**
   * How it grew.
   */
  growth: StoryStatement[];
  /**
   * Periods of inactivity.
   */
  inactivity: StoryStatement[];
  /**
   * How the project started.
   */
  origin: StoryStatement[];
  /**
   * Major rewrites and restructurings.
   */
  rewrites: StoryStatement[];
}
/**
 * Inferred architecture of the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ArchitectureReport".
 */
export interface ArchitectureReport1 {
  /**
   * Dependency cycles.
   */
  cycles: DependencyCycle[];
  /**
   * External packages imported by the code.
   */
  external: ExternalImport[];
  /**
   * File-level edges (capped; see `fileEdgesTruncated`).
   */
  fileEdges: FileEdge[];
  /**
   * `true` when `fileEdges` was capped.
   */
  fileEdgesTruncated?: boolean;
  /**
   * Modules with no internal edges in either direction.
   */
  isolatedModules: string[];
  /**
   * Layers of the condensed module graph.
   */
  layers: Layer[];
  /**
   * Description of the inference method.
   */
  method: string;
  /**
   * Module-level edges.
   */
  moduleEdges: ModuleEdge[];
  /**
   * Modules, sorted by identifier.
   */
  modules: ModuleRecord[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Package boundaries (monorepo members).
   */
  packages: PackageBoundary[];
  /**
   * Import statements resolved to repository files.
   */
  resolvedImports: number;
  /**
   * Signals that led to the characterization.
   */
  signals: ArchitectureSignal[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Short characterization such as `Modular` or `Layered`; always labeled as inferred.
   */
  style: string;
  /**
   * Confidence of the characterization.
   */
  styleConfidence: "unavailable" | "low" | "medium" | "high";
  /**
   * Import statements that referenced neither a repository file nor a known package.
   */
  unresolvedImports: number;
  /**
   * Workspace configuration, when present.
   */
  workspace?: WorkspaceInfo | null;
}
/**
 * Build systems, commands, environment, and CI.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "BuildReport".
 */
export interface BuildReport1 {
  /**
   * CI/CD configurations.
   */
  ci: CiConfig[];
  /**
   * Build, run, and development commands derived from metadata.
   */
  commands: CommandCandidate[];
  /**
   * Configuration files and their purpose.
   */
  configurationFiles: ConfigFileInfo[];
  /**
   * Container definitions.
   */
  containers: string[];
  /**
   * Results of explicitly authorized build runs.
   */
  executions: ExecutionResult[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Detected environment requirements.
   */
  requirements: EnvironmentRequirement[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Detected build systems.
   */
  systems: DetectedTool[];
}
/**
 * Complexity signals across the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ComplexityReport".
 */
export interface ComplexityReport1 {
  /**
   * Mean approximate cyclomatic complexity.
   */
  averageCyclomatic: number;
  /**
   * Statistics per language.
   */
  byLanguage: LanguageComplexity[];
  /**
   * Distribution of function complexity.
   */
  distribution: DistributionBucket[];
  /**
   * Functions analyzed.
   */
  functionsAnalyzed: number;
  /**
   * Median approximate cyclomatic complexity.
   */
  medianCyclomatic: number;
  /**
   * How complexity was approximated.
   */
  method: string;
  /**
   * Most complex functions, descending.
   */
  topFunctions: FunctionSignal[];
}
/**
 * Dependency analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DependencyReport".
 */
export interface DependencyReport {
  advisories: AdvisoryStatus;
  /**
   * Most frequently imported external packages.
   */
  concentration: DependencyConcentration[];
  /**
   * Direct dependencies, sorted by ecosystem and name.
   */
  dependencies: DependencyRecord[];
  /**
   * Number of distinct direct dependencies.
   */
  directCount: number;
  /**
   * Packages resolved at multiple versions.
   */
  duplicates: DuplicateVersions[];
  /**
   * Counts per ecosystem.
   */
  ecosystems: EcosystemSummary[];
  /**
   * Number of distinct packages pinned in lockfiles (direct and transitive).
   */
  lockedCount: number;
  /**
   * Lockfiles with consistency information.
   */
  lockfiles: LockfileRecord[];
  /**
   * Manifests and lockfiles found.
   */
  manifests: ManifestRecord[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Long-unchanged manifests.
   */
  staleSignals: StaleSignal[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Compact identity and characterization of a repository snapshot.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DnaFingerprint".
 */
export interface DnaFingerprint1 {
  /**
   * Characterization dimensions.
   */
  dimensions: DnaDimension[];
  /**
   * Deterministic snapshot identifier: `rdna1-` followed by 32 hexadecimal characters.
   * It identifies content; it is not a security primitive.
   */
  dnaHash: string;
  /**
   * How the hash and dimensions were computed.
   */
  method: string;
}
/**
 * Documentation analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DocsReport".
 */
export interface DocsReport1 {
  /**
   * Documentation checklist.
   */
  checks: DocCheck[];
  /**
   * Purpose statement quoted from the README or a manifest, when found.
   */
  description?: string | null;
  /**
   * Where `description` was taken from.
   */
  descriptionSource?: string | null;
  /**
   * Directories dedicated to documentation.
   */
  docDirectories: string[];
  /**
   * Documentation files.
   */
  docFiles: number;
  /**
   * Lines in documentation files.
   */
  docLines: number;
  /**
   * Example files or directories.
   */
  examples: string[];
  /**
   * Detected license.
   */
  license?: LicenseInfo | null;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * README structure.
   */
  readme?: ReadmeInfo | null;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Duplication analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "DuplicationReport".
 */
export interface DuplicationReport1 {
  /**
   * Code lines considered for duplication.
   */
  analyzedLines: number;
  /**
   * Duplicate clusters, largest first.
   */
  clusters: DuplicateCluster[];
  /**
   * Lines covered by at least one duplicated block (excluding the first occurrence).
   */
  duplicatedLines: number;
  /**
   * How duplication was detected.
   */
  method: string;
  /**
   * Minimum block size in normalized tokens.
   */
  minTokens: number;
  /**
   * `duplicatedLines / analyzedLines`.
   */
  ratio: number;
  /**
   * Whether duplication detection ran.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Evolution analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "EvolutionReport".
 */
export interface EvolutionReport1 {
  /**
   * Directories that stopped changing.
   */
  abandonedAreas: AbandonedArea[];
  archaeology: ArchaeologyReport;
  /**
   * Development epochs, oldest first.
   */
  epochs: Epoch[];
  /**
   * Historical events, oldest first.
   */
  events: EvolutionEvent[];
  /**
   * Growth curve derived from snapshots.
   */
  growth: GrowthPoint[];
  /**
   * Module ages.
   */
  moduleAges: ModuleAge[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * How snapshots were sampled.
   */
  sampling: string;
  /**
   * Time Machine snapshots, oldest first.
   */
  snapshots: Snapshot[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Chronological Project Story.
   */
  story: StoryStatement[];
}
/**
 * Git history analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "GitReport".
 */
export interface GitReport1 {
  activity: ActivitySummary;
  /**
   * Branches.
   */
  branches: BranchInfo[];
  /**
   * Number of commits analyzed (reachable from HEAD).
   */
  commitCount: number;
  /**
   * Most recent commits (capped by configuration).
   */
  commits: CommitRecord[];
  /**
   * Contributors sorted by commits, descending.
   */
  contributors: ContributorRecord[];
  /**
   * Branch checked out at analysis time.
   */
  currentBranch?: string | null;
  /**
   * Daily commit activity (days with at least one commit).
   */
  dailyActivity: DailyActivity[];
  /**
   * Default branch, when it can be determined.
   */
  defaultBranch?: string | null;
  /**
   * History per directory.
   */
  directoryActivity: DirectoryActivity[];
  /**
   * Gaps without commits longer than the configured threshold.
   */
  dormantPeriods: DormantPeriod[];
  /**
   * History per current file, sorted by path.
   */
  fileHistory: FileHistoryRecord[];
  /**
   * Earliest analyzed commit.
   */
  firstCommit?: CommitRef | null;
  /**
   * Git version used for the analysis.
   */
  gitVersion?: string | null;
  /**
   * Commit analyzed.
   */
  head?: CommitRef | null;
  /**
   * `true` when the configured commit limit was reached.
   */
  historyTruncated?: boolean;
  /**
   * Ranked hotspots.
   */
  hotSpots: Hotspot[];
  /**
   * Latest analyzed commit.
   */
  lastCommit?: CommitRef | null;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  ownership: OwnershipSummary;
  /**
   * Size of the "recent" window used for recency signals, in days before the latest commit.
   */
  recentWindowDays: number;
  /**
   * Tags that look like release versions, oldest first.
   */
  releases: ReleaseInfo[];
  /**
   * `true` when the clone is shallow and history is incomplete.
   */
  shallow?: boolean;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Tags.
   */
  tags: TagInfo[];
  /**
   * Monthly commit activity.
   */
  timeline: TimelineBucket[];
  /**
   * Commits per weekday (0 = Monday) and hour (0–23), in the author's local time.
   */
  weekdayHour: number[][];
}
/**
 * The analyzed input, described without revealing local absolute paths.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "InputInfo".
 */
export interface InputInfo1 {
  /**
   * Safe display string: a directory name, a credential-free URL, or an archive file name.
   */
  display: string;
  /**
   * Input kind.
   */
  kind: "local-directory" | "git-repository" | "git-url" | "archive" | "artifact";
}
/**
 * Human-oriented insights computed from the analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Insights".
 */
export interface Insights1 {
  /**
   * Answers to the "first look" questions.
   */
  firstLook: QuestionAnswer[];
  /**
   * Repository-specific terminology.
   */
  glossary: GlossaryTerm[];
  /**
   * Files worth reading first.
   */
  importantFiles: ImportantFile[];
  /**
   * Suggested onboarding path.
   */
  onboarding: GuideStep[];
  /**
   * What changed recently.
   */
  recentChanges?: RecentChanges | null;
}
/**
 * Language composition of the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "LanguageReport".
 */
export interface LanguageReport1 {
  /**
   * Cross-language relationships.
   */
  interactions: LanguageInteraction[];
  /**
   * Languages sorted by code lines, descending.
   */
  languages: LanguageStat[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Identifiers of the primary languages (up to three, by share).
   */
  primary: string[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Text files whose language was not recognized.
   */
  unrecognizedFiles: number;
}
/**
 * Marker comments such as TODO and FIXME.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MarkerReport".
 */
export interface MarkerReport1 {
  /**
   * Counts per kind.
   */
  counts: MarkerCount[];
  /**
   * Individual markers (capped).
   */
  items: MarkerItem[];
  /**
   * Total markers found.
   */
  total: number;
}
/**
 * All measurements of the analysis.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "MetricsReport".
 */
export interface MetricsReport1 {
  /**
   * Confidence per section.
   */
  confidence: SectionConfidence[];
  /**
   * Raw metrics, sorted by identifier.
   */
  raw: Metric[];
  /**
   * Normalized signals.
   */
  signals: NormalizedSignal[];
}
/**
 * How concentrated commits are among contributors.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "OwnershipSummary".
 */
export interface OwnershipSummary1 {
  /**
   * Number of contributors.
   */
  contributors: number;
  /**
   * Fewest contributors who together authored at least half of all commits.
   */
  contributorsForHalfOfCommits: number;
  /**
   * Neutral explanation of the numbers.
   */
  note: string;
  /**
   * Share of commits by the most active contributor (0–1).
   */
  topContributorShare: number;
}
/**
 * The platform the analysis ran on.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PlatformInfo".
 */
export interface PlatformInfo1 {
  /**
   * CPU architecture, e.g. `x86_64`.
   */
  arch: string;
  /**
   * OS family, e.g. `unix`.
   */
  family: string;
  /**
   * Operating system, e.g. `linux`.
   */
  os: string;
}
/**
 * Privacy facts about the analysis and the document.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "PrivacyInfo".
 */
export interface PrivacyInfo1 {
  /**
   * `true` when the analysis used the network (for example to clone a URL).
   */
  networkUsed: boolean;
  /**
   * Privacy preset applied to this document.
   */
  preset: "local" | "share" | "public";
  /**
   * Redactions applied to this document, e.g. `contributor names anonymized`.
   */
  redactions: string[];
  /**
   * `true` when a remote AI provider received data.
   */
  remoteAi: boolean;
  /**
   * Always `false`: RepoDNA collects no telemetry.
   */
  telemetry: boolean;
}
/**
 * Code-quality signals.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "QualityReport".
 */
export interface QualityReport1 {
  complexity: ComplexityReport;
  /**
   * Dead-code candidates.
   */
  deadCodeCandidates: DeadCodeCandidate[];
  /**
   * Functions with deep nesting.
   */
  deepNesting: FunctionSignal[];
  duplication: DuplicationReport;
  /**
   * Files above the large-file threshold.
   */
  largeFiles: FileSignal[];
  /**
   * Functions above the large-function threshold.
   */
  largeFunctions: FunctionSignal[];
  /**
   * Maintainability indicators.
   */
  maintainability: MaintainabilitySignal[];
  markers: MarkerReport;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Who and what the repository is.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "RepositoryIdentity".
 */
export interface RepositoryIdentity1 {
  /**
   * Default branch, when known.
   */
  defaultBranch?: string | null;
  /**
   * Purpose statement quoted from the README or a manifest.
   */
  description?: string | null;
  /**
   * `true` when Git metadata was available.
   */
  isGitRepository: boolean;
  /**
   * Detected license.
   */
  license?: LicenseInfo | null;
  /**
   * Repository name (from the remote URL or the directory name).
   */
  name: string;
  /**
   * Owner or namespace from the remote URL, when available.
   */
  owner?: string | null;
  /**
   * Primary language identifiers.
   */
  primaryLanguages: string[];
  /**
   * Remotes with credentials removed.
   */
  remotes: RemoteInfo[];
  /**
   * Commit analyzed, when the repository uses Git.
   */
  revision?: string | null;
  /**
   * Size class by file count.
   */
  sizeClass: "tiny" | "small" | "medium" | "large" | "very-large";
}
/**
 * Security signals.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SecurityReport".
 */
export interface SecurityReport1 {
  advisories: AdvisoryStatus1;
  /**
   * What these signals do and do not mean.
   */
  disclaimer: string;
  /**
   * Text files scanned.
   */
  filesScanned: number;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Potentially dangerous patterns.
   */
  patterns: PatternCandidate[];
  /**
   * Unusual file permissions.
   */
  permissions: PermissionSignal[];
  /**
   * Rules applied.
   */
  rules: SecurityRuleInfo[];
  /**
   * Possible credentials (values redacted).
   */
  secrets: SecretCandidate[];
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
}
/**
 * Similarity between files inside the repository.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "SimilarityReport".
 */
export interface SimilarityReport1 {
  /**
   * How similarity was estimated.
   */
  method: string;
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * File pairs above the similarity threshold, most similar first.
   */
  similarFiles: SimilarFilePair[];
  /**
   * Whether similarity analysis ran.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Threshold used (0–1).
   */
  threshold: number;
}
/**
 * Repository layout, file classification, and size.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "StructureReport".
 */
export interface StructureReport1 {
  /**
   * Number of binary files.
   */
  binaryFiles: number;
  /**
   * Blank lines across text files.
   */
  blankLines: number;
  /**
   * File counts per category.
   */
  categories: CategoryCount[];
  /**
   * Code lines across text files.
   */
  codeLines: number;
  /**
   * Comment-only lines across text files.
   */
  commentLines: number;
  /**
   * Aggregated directory statistics, sorted by path.
   */
  directories: DirectoryRecord[];
  /**
   * Detected entrypoints.
   */
  entrypoints: Entrypoint[];
  /**
   * Every discovered file, sorted by path.
   */
  files: FileRecord[];
  /**
   * Number of files flagged as generated.
   */
  generatedFiles: number;
  /**
   * Ignore patterns applied in addition to `.gitignore` rules.
   */
  ignorePatterns?: string[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Size class by file count.
   */
  sizeClass: "tiny" | "small" | "medium" | "large" | "very-large";
  /**
   * Number of files whose content analysis was skipped.
   */
  skippedFiles: number;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Detected symbols (capped by configuration; see `truncated`).
   */
  symbols: SymbolRecord[];
  /**
   * Number of symbolic links encountered (never followed).
   */
  symlinks: number;
  /**
   * Total size of discovered files in bytes.
   */
  totalBytes: number;
  /**
   * Number of files discovered (after ignore rules).
   */
  totalFiles: number;
  /**
   * Total lines across text files.
   */
  totalLines: number;
  /**
   * `true` when lists in this section were capped to keep the artifact manageable.
   */
  truncated?: boolean;
  /**
   * Number of files flagged as vendored.
   */
  vendoredFiles: number;
}
/**
 * Test detection.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "TestReport".
 */
export interface TestReport1 {
  /**
   * Test commands found in CI configuration.
   */
  ciCommands: CommandCandidate[];
  /**
   * Test commands derived from metadata.
   */
  commands: CommandCandidate[];
  /**
   * Coverage reports present in the repository.
   */
  coverageArtifacts: string[];
  /**
   * Results of explicitly authorized test runs.
   */
  executions: ExecutionResult[];
  /**
   * Detected test frameworks.
   */
  frameworks: DetectedTool[];
  /**
   * Source files that also contain tests, such as Rust `#[cfg(test)]` modules. Their
   * code is counted as source code.
   */
  inlineTestFiles: number;
  /**
   * Kinds of tests detected.
   */
  kinds: TestKindSignal[];
  /**
   * Notes about limitations or partial results.
   */
  notes?: string[];
  /**
   * Files classified as first-party source code.
   */
  sourceFiles: number;
  /**
   * Whether this section was analyzed.
   */
  status: "analyzed" | "partial" | "skipped" | "unavailable";
  /**
   * Directories that contain tests.
   */
  testDirectories: string[];
  /**
   * Files classified as tests.
   */
  testFiles: number;
  /**
   * Code lines in test files.
   */
  testLines: number;
  /**
   * `testLines / (testLines + sourceCodeLines)` (0–1).
   */
  testRatio: number;
}
/**
 * Thresholds used by analyzers to turn measurements into signals.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "Thresholds".
 */
export interface Thresholds1 {
  /**
   * Block nesting depth above which a function is reported.
   */
  deep_nesting: number;
  /**
   * Share of internal module edges (0–1) pointing at one module above which a
   * dependency-concentration signal is reported.
   */
  dependency_concentration: number;
  /**
   * Minimum gap in days to report a dormant period.
   */
  dormant_days: number;
  /**
   * Minimum duplicated block size in normalized tokens.
   */
  duplicate_min_tokens: number;
  /**
   * Share of recent churn (0–1) above which a directory is reported as a high-churn area.
   */
  high_churn_share: number;
  /**
   * Approximate cyclomatic complexity above which a function is reported.
   */
  high_complexity: number;
  /**
   * Minimum commits for a file to be considered as a hotspot.
   */
  hotspot_min_commits: number;
  /**
   * Size in bytes above which a committed binary file is reported.
   */
  large_binary_bytes: number;
  /**
   * Code lines above which a file is reported as large.
   */
  large_file_lines: number;
  /**
   * Lines above which a function is reported as large.
   */
  large_function_lines: number;
  /**
   * Size of the "recent" window in days (before the latest commit).
   */
  recent_days: number;
  /**
   * Minimum estimated Jaccard similarity (0–1) for similar-file pairs.
   */
  similarity: number;
  /**
   * Days a manifest must be unchanged before a stale-declarations signal is reported.
   */
  stale_manifest_days: number;
}
/**
 * The tool that produced an artifact.
 *
 * This interface was referenced by `RepositoryDna`'s JSON-Schema
 * via the `definition` "ToolInfo".
 */
export interface ToolInfo1 {
  /**
   * Always `RepoDNA`.
   */
  name: string;
  /**
   * Artifact schema version, e.g. `1.0`.
   */
  schemaVersion: string;
  /**
   * Tool version, e.g. `1.0.0`.
   */
  version: string;
}
